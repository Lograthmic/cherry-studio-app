import { Calculator as CalculatorIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Calculator icon.
 * @see https://lucide.dev/icons/calculator
 */
export default iconWithClassName(CalculatorIconBase);
