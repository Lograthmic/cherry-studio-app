import { Fuel as FuelIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Fuel icon.
 * @see https://lucide.dev/icons/fuel
 */
export default iconWithClassName(FuelIconBase);
