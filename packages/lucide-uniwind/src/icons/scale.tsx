import { Scale as ScaleIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Scale icon.
 * @see https://lucide.dev/icons/scale
 */
export default iconWithClassName(ScaleIconBase);
