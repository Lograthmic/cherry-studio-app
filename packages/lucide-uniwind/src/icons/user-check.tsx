import { UserCheck as UserCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * UserCheck icon.
 * @see https://lucide.dev/icons/user-check
 */
export default iconWithClassName(UserCheckIconBase);
