import { Tablets as TabletsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Tablets icon.
 * @see https://lucide.dev/icons/tablets
 */
export default iconWithClassName(TabletsIconBase);
