import { Signature as SignatureIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Signature icon.
 * @see https://lucide.dev/icons/signature
 */
export default iconWithClassName(SignatureIconBase);
