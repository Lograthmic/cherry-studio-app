import { Quote as QuoteIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Quote icon.
 * @see https://lucide.dev/icons/quote
 */
export default iconWithClassName(QuoteIconBase);
