import { FormInput as FormInputIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FormInput icon.
 * @see https://lucide.dev/icons/form-input
 */
export default iconWithClassName(FormInputIconBase);
