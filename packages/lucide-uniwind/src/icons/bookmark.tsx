import { Bookmark as BookmarkIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bookmark icon.
 * @see https://lucide.dev/icons/bookmark
 */
export default iconWithClassName(BookmarkIconBase);
