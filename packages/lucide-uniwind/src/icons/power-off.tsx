import { PowerOff as PowerOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PowerOff icon.
 * @see https://lucide.dev/icons/power-off
 */
export default iconWithClassName(PowerOffIconBase);
