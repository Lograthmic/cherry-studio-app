import { FolderTree as FolderTreeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderTree icon.
 * @see https://lucide.dev/icons/folder-tree
 */
export default iconWithClassName(FolderTreeIconBase);
