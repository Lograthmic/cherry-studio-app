import { Replace as ReplaceIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Replace icon.
 * @see https://lucide.dev/icons/replace
 */
export default iconWithClassName(ReplaceIconBase);
