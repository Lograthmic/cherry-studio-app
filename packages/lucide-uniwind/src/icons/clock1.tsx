import { Clock1 as Clock1IconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Clock1 icon.
 * @see https://lucide.dev/icons/clock1
 */
export default iconWithClassName(Clock1IconBase);
