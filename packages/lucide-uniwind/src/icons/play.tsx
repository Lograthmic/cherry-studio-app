import { Play as PlayIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Play icon.
 * @see https://lucide.dev/icons/play
 */
export default iconWithClassName(PlayIconBase);
