import { Church as ChurchIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Church icon.
 * @see https://lucide.dev/icons/church
 */
export default iconWithClassName(ChurchIconBase);
