import { Command as CommandIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Command icon.
 * @see https://lucide.dev/icons/command
 */
export default iconWithClassName(CommandIconBase);
