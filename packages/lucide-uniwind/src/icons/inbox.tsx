import { Inbox as InboxIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Inbox icon.
 * @see https://lucide.dev/icons/inbox
 */
export default iconWithClassName(InboxIconBase);
