import { Rewind as RewindIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Rewind icon.
 * @see https://lucide.dev/icons/rewind
 */
export default iconWithClassName(RewindIconBase);
