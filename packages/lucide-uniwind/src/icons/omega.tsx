import { Omega as OmegaIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Omega icon.
 * @see https://lucide.dev/icons/omega
 */
export default iconWithClassName(OmegaIconBase);
