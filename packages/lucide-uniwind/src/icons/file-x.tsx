import { FileX as FileXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileX icon.
 * @see https://lucide.dev/icons/file-x
 */
export default iconWithClassName(FileXIconBase);
