import { Variable as VariableIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Variable icon.
 * @see https://lucide.dev/icons/variable
 */
export default iconWithClassName(VariableIconBase);
