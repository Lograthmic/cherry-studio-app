import { TrainFront as TrainFrontIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TrainFront icon.
 * @see https://lucide.dev/icons/train-front
 */
export default iconWithClassName(TrainFrontIconBase);
