import { SignalLow as SignalLowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SignalLow icon.
 * @see https://lucide.dev/icons/signal-low
 */
export default iconWithClassName(SignalLowIconBase);
