import { MessageCircleCheck as MessageCircleCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MessageCircleCheck icon.
 * @see https://lucide.dev/icons/message-circle-check
 */
export default iconWithClassName(MessageCircleCheckIconBase);
