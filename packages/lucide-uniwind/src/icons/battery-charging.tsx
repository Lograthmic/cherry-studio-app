import { BatteryCharging as BatteryChargingIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BatteryCharging icon.
 * @see https://lucide.dev/icons/battery-charging
 */
export default iconWithClassName(BatteryChargingIconBase);
