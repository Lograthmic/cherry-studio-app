import { Archive as ArchiveIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Archive icon.
 * @see https://lucide.dev/icons/archive
 */
export default iconWithClassName(ArchiveIconBase);
