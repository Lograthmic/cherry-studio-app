import { StopCircle as StopCircleIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * StopCircle icon.
 * @see https://lucide.dev/icons/stop-circle
 */
export default iconWithClassName(StopCircleIconBase);
