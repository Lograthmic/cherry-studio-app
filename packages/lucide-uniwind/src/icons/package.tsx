import { Package as PackageIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Package icon.
 * @see https://lucide.dev/icons/package
 */
export default iconWithClassName(PackageIconBase);
