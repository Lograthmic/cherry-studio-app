import { ClockCheck as ClockCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ClockCheck icon.
 * @see https://lucide.dev/icons/clock-check
 */
export default iconWithClassName(ClockCheckIconBase);
