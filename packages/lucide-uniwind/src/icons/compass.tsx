import { Compass as CompassIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Compass icon.
 * @see https://lucide.dev/icons/compass
 */
export default iconWithClassName(CompassIconBase);
