import { Receipt as ReceiptIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Receipt icon.
 * @see https://lucide.dev/icons/receipt
 */
export default iconWithClassName(ReceiptIconBase);
