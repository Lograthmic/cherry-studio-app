import { FolderInput as FolderInputIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderInput icon.
 * @see https://lucide.dev/icons/folder-input
 */
export default iconWithClassName(FolderInputIconBase);
