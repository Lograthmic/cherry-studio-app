import { Import as ImportIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Import icon.
 * @see https://lucide.dev/icons/import
 */
export default iconWithClassName(ImportIconBase);
