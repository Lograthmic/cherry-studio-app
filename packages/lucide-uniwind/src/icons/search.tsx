import { Search as SearchIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Search icon.
 * @see https://lucide.dev/icons/search
 */
export default iconWithClassName(SearchIconBase);
