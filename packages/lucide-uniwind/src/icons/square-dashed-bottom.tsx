import { SquareDashedBottom as SquareDashedBottomIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SquareDashedBottom icon.
 * @see https://lucide.dev/icons/square-dashed-bottom
 */
export default iconWithClassName(SquareDashedBottomIconBase);
