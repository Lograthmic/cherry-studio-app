import { BellRing as BellRingIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BellRing icon.
 * @see https://lucide.dev/icons/bell-ring
 */
export default iconWithClassName(BellRingIconBase);
