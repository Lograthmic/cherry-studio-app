import { Scan as ScanIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Scan icon.
 * @see https://lucide.dev/icons/scan
 */
export default iconWithClassName(ScanIconBase);
