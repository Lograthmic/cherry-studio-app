import { PrinterCheck as PrinterCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PrinterCheck icon.
 * @see https://lucide.dev/icons/printer-check
 */
export default iconWithClassName(PrinterCheckIconBase);
