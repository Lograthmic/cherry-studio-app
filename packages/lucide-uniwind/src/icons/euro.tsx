import { Euro as EuroIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Euro icon.
 * @see https://lucide.dev/icons/euro
 */
export default iconWithClassName(EuroIconBase);
