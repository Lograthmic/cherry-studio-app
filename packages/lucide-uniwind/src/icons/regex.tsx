import { Regex as RegexIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Regex icon.
 * @see https://lucide.dev/icons/regex
 */
export default iconWithClassName(RegexIconBase);
