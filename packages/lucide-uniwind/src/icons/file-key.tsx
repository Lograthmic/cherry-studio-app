import { FileKey as FileKeyIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileKey icon.
 * @see https://lucide.dev/icons/file-key
 */
export default iconWithClassName(FileKeyIconBase);
