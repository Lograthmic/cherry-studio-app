import { Book as BookIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Book icon.
 * @see https://lucide.dev/icons/book
 */
export default iconWithClassName(BookIconBase);
