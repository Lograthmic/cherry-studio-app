import { Train as TrainIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Train icon.
 * @see https://lucide.dev/icons/train
 */
export default iconWithClassName(TrainIconBase);
