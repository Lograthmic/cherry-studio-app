import { BookAudio as BookAudioIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookAudio icon.
 * @see https://lucide.dev/icons/book-audio
 */
export default iconWithClassName(BookAudioIconBase);
