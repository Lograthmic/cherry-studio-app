import { Cog as CogIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cog icon.
 * @see https://lucide.dev/icons/cog
 */
export default iconWithClassName(CogIconBase);
