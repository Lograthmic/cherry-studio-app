import { Lock as LockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Lock icon.
 * @see https://lucide.dev/icons/lock
 */
export default iconWithClassName(LockIconBase);
