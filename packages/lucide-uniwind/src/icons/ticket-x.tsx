import { TicketX as TicketXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TicketX icon.
 * @see https://lucide.dev/icons/ticket-x
 */
export default iconWithClassName(TicketXIconBase);
