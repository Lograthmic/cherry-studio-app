import { ListVideo as ListVideoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListVideo icon.
 * @see https://lucide.dev/icons/list-video
 */
export default iconWithClassName(ListVideoIconBase);
