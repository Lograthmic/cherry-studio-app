import { Move as MoveIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Move icon.
 * @see https://lucide.dev/icons/move
 */
export default iconWithClassName(MoveIconBase);
