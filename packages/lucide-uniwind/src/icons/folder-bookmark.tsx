import { FolderBookmark as FolderBookmarkIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderBookmark icon.
 * @see https://lucide.dev/icons/folder-bookmark
 */
export default iconWithClassName(FolderBookmarkIconBase);
