import { Plug as PlugIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Plug icon.
 * @see https://lucide.dev/icons/plug
 */
export default iconWithClassName(PlugIconBase);
