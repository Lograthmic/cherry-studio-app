import { Filter as FilterIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Filter icon.
 * @see https://lucide.dev/icons/filter
 */
export default iconWithClassName(FilterIconBase);
