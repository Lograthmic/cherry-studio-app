import { Snowflake as SnowflakeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Snowflake icon.
 * @see https://lucide.dev/icons/snowflake
 */
export default iconWithClassName(SnowflakeIconBase);
