import { CakeSlice as CakeSliceIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CakeSlice icon.
 * @see https://lucide.dev/icons/cake-slice
 */
export default iconWithClassName(CakeSliceIconBase);
