import { TimerOff as TimerOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TimerOff icon.
 * @see https://lucide.dev/icons/timer-off
 */
export default iconWithClassName(TimerOffIconBase);
