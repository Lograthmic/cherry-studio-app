import { UmbrellaOff as UmbrellaOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * UmbrellaOff icon.
 * @see https://lucide.dev/icons/umbrella-off
 */
export default iconWithClassName(UmbrellaOffIconBase);
