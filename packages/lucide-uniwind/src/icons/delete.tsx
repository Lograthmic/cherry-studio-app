import { Delete as DeleteIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Delete icon.
 * @see https://lucide.dev/icons/delete
 */
export default iconWithClassName(DeleteIconBase);
