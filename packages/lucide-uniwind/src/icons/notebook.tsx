import { Notebook as NotebookIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Notebook icon.
 * @see https://lucide.dev/icons/notebook
 */
export default iconWithClassName(NotebookIconBase);
