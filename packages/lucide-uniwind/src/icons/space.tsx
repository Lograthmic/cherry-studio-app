import { Space as SpaceIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Space icon.
 * @see https://lucide.dev/icons/space
 */
export default iconWithClassName(SpaceIconBase);
