import { PackageOpen as PackageOpenIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PackageOpen icon.
 * @see https://lucide.dev/icons/package-open
 */
export default iconWithClassName(PackageOpenIconBase);
