import { Aperture as ApertureIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Aperture icon.
 * @see https://lucide.dev/icons/aperture
 */
export default iconWithClassName(ApertureIconBase);
