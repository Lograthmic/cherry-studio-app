import { Ruler as RulerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Ruler icon.
 * @see https://lucide.dev/icons/ruler
 */
export default iconWithClassName(RulerIconBase);
