import { Pin as PinIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pin icon.
 * @see https://lucide.dev/icons/pin
 */
export default iconWithClassName(PinIconBase);
