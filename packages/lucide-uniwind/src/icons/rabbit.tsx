import { Rabbit as RabbitIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Rabbit icon.
 * @see https://lucide.dev/icons/rabbit
 */
export default iconWithClassName(RabbitIconBase);
