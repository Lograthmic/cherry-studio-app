import { Pi as PiIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pi icon.
 * @see https://lucide.dev/icons/pi
 */
export default iconWithClassName(PiIconBase);
