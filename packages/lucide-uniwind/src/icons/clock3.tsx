import { Clock3 as Clock3IconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Clock3 icon.
 * @see https://lucide.dev/icons/clock3
 */
export default iconWithClassName(Clock3IconBase);
