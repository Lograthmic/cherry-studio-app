import { CalendarClock as CalendarClockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CalendarClock icon.
 * @see https://lucide.dev/icons/calendar-clock
 */
export default iconWithClassName(CalendarClockIconBase);
