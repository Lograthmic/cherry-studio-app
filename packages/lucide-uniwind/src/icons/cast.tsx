import { Cast as CastIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cast icon.
 * @see https://lucide.dev/icons/cast
 */
export default iconWithClassName(CastIconBase);
