import { FileStack as FileStackIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileStack icon.
 * @see https://lucide.dev/icons/file-stack
 */
export default iconWithClassName(FileStackIconBase);
