import { Minimize as MinimizeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Minimize icon.
 * @see https://lucide.dev/icons/minimize
 */
export default iconWithClassName(MinimizeIconBase);
