import { Radius as RadiusIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Radius icon.
 * @see https://lucide.dev/icons/radius
 */
export default iconWithClassName(RadiusIconBase);
