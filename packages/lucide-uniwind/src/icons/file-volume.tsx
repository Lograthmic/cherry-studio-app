import { FileVolume as FileVolumeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileVolume icon.
 * @see https://lucide.dev/icons/file-volume
 */
export default iconWithClassName(FileVolumeIconBase);
