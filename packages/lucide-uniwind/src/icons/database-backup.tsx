import { DatabaseBackup as DatabaseBackupIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * DatabaseBackup icon.
 * @see https://lucide.dev/icons/database-backup
 */
export default iconWithClassName(DatabaseBackupIconBase);
