import { XLineTop as XLineTopIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * XLineTop icon.
 * @see https://lucide.dev/icons/xline-top
 */
export default iconWithClassName(XLineTopIconBase);
