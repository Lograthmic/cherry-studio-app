import { RemoveFormatting as RemoveFormattingIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * RemoveFormatting icon.
 * @see https://lucide.dev/icons/remove-formatting
 */
export default iconWithClassName(RemoveFormattingIconBase);
