import { TextSelection as TextSelectionIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TextSelection icon.
 * @see https://lucide.dev/icons/text-selection
 */
export default iconWithClassName(TextSelectionIconBase);
