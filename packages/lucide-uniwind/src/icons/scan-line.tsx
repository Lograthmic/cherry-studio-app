import { ScanLine as ScanLineIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ScanLine icon.
 * @see https://lucide.dev/icons/scan-line
 */
export default iconWithClassName(ScanLineIconBase);
