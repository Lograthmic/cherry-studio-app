import { Network as NetworkIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Network icon.
 * @see https://lucide.dev/icons/network
 */
export default iconWithClassName(NetworkIconBase);
