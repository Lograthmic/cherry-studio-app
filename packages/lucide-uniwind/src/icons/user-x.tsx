import { UserX as UserXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * UserX icon.
 * @see https://lucide.dev/icons/user-x
 */
export default iconWithClassName(UserXIconBase);
