import { Sigma as SigmaIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Sigma icon.
 * @see https://lucide.dev/icons/sigma
 */
export default iconWithClassName(SigmaIconBase);
