import { Verified as VerifiedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Verified icon.
 * @see https://lucide.dev/icons/verified
 */
export default iconWithClassName(VerifiedIconBase);
