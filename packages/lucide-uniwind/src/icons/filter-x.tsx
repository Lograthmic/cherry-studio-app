import { FilterX as FilterXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FilterX icon.
 * @see https://lucide.dev/icons/filter-x
 */
export default iconWithClassName(FilterXIconBase);
