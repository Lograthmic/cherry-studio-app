import { Option as OptionIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Option icon.
 * @see https://lucide.dev/icons/option
 */
export default iconWithClassName(OptionIconBase);
