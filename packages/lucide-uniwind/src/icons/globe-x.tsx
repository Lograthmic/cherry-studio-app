import { GlobeX as GlobeXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * GlobeX icon.
 * @see https://lucide.dev/icons/globe-x
 */
export default iconWithClassName(GlobeXIconBase);
