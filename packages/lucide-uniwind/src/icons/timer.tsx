import { Timer as TimerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Timer icon.
 * @see https://lucide.dev/icons/timer
 */
export default iconWithClassName(TimerIconBase);
