import { Type as TypeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Type icon.
 * @see https://lucide.dev/icons/type
 */
export default iconWithClassName(TypeIconBase);
