import { Building as BuildingIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Building icon.
 * @see https://lucide.dev/icons/building
 */
export default iconWithClassName(BuildingIconBase);
