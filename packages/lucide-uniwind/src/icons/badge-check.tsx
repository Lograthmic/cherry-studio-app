import { BadgeCheck as BadgeCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BadgeCheck icon.
 * @see https://lucide.dev/icons/badge-check
 */
export default iconWithClassName(BadgeCheckIconBase);
