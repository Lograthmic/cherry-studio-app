import { CloudSnow as CloudSnowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CloudSnow icon.
 * @see https://lucide.dev/icons/cloud-snow
 */
export default iconWithClassName(CloudSnowIconBase);
