import { BookX as BookXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookX icon.
 * @see https://lucide.dev/icons/book-x
 */
export default iconWithClassName(BookXIconBase);
