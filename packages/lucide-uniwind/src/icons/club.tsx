import { Club as ClubIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Club icon.
 * @see https://lucide.dev/icons/club
 */
export default iconWithClassName(ClubIconBase);
