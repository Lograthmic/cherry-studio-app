import { Baseline as BaselineIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Baseline icon.
 * @see https://lucide.dev/icons/baseline
 */
export default iconWithClassName(BaselineIconBase);
