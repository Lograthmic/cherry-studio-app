import { FileInput as FileInputIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileInput icon.
 * @see https://lucide.dev/icons/file-input
 */
export default iconWithClassName(FileInputIconBase);
