import { Speech as SpeechIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Speech icon.
 * @see https://lucide.dev/icons/speech
 */
export default iconWithClassName(SpeechIconBase);
