import { PhoneCall as PhoneCallIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PhoneCall icon.
 * @see https://lucide.dev/icons/phone-call
 */
export default iconWithClassName(PhoneCallIconBase);
