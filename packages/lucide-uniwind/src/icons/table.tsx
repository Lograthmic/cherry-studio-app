import { Table as TableIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Table icon.
 * @see https://lucide.dev/icons/table
 */
export default iconWithClassName(TableIconBase);
