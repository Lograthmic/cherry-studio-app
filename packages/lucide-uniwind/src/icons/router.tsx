import { Router as RouterIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Router icon.
 * @see https://lucide.dev/icons/router
 */
export default iconWithClassName(RouterIconBase);
