import { Barcode as BarcodeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Barcode icon.
 * @see https://lucide.dev/icons/barcode
 */
export default iconWithClassName(BarcodeIconBase);
