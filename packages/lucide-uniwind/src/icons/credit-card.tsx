import { CreditCard as CreditCardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CreditCard icon.
 * @see https://lucide.dev/icons/credit-card
 */
export default iconWithClassName(CreditCardIconBase);
