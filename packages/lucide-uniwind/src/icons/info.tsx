import { Info as InfoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Info icon.
 * @see https://lucide.dev/icons/info
 */
export default iconWithClassName(InfoIconBase);
