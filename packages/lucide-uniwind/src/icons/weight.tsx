import { Weight as WeightIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Weight icon.
 * @see https://lucide.dev/icons/weight
 */
export default iconWithClassName(WeightIconBase);
