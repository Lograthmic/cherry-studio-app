import { AppWindowMac as AppWindowMacIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * AppWindowMac icon.
 * @see https://lucide.dev/icons/app-window-mac
 */
export default iconWithClassName(AppWindowMacIconBase);
