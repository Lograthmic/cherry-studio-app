import { Signal as SignalIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Signal icon.
 * @see https://lucide.dev/icons/signal
 */
export default iconWithClassName(SignalIconBase);
