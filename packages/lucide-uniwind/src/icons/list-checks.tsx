import { ListChecks as ListChecksIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListChecks icon.
 * @see https://lucide.dev/icons/list-checks
 */
export default iconWithClassName(ListChecksIconBase);
