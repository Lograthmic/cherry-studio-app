import { Tickets as TicketsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Tickets icon.
 * @see https://lucide.dev/icons/tickets
 */
export default iconWithClassName(TicketsIconBase);
