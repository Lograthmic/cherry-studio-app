import { Star as StarIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Star icon.
 * @see https://lucide.dev/icons/star
 */
export default iconWithClassName(StarIconBase);
