import { Forward as ForwardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Forward icon.
 * @see https://lucide.dev/icons/forward
 */
export default iconWithClassName(ForwardIconBase);
