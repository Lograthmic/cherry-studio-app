import { Factory as FactoryIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Factory icon.
 * @see https://lucide.dev/icons/factory
 */
export default iconWithClassName(FactoryIconBase);
