import { ScanText as ScanTextIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ScanText icon.
 * @see https://lucide.dev/icons/scan-text
 */
export default iconWithClassName(ScanTextIconBase);
