import { ServerCrash as ServerCrashIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ServerCrash icon.
 * @see https://lucide.dev/icons/server-crash
 */
export default iconWithClassName(ServerCrashIconBase);
