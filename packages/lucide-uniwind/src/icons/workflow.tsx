import { Workflow as WorkflowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Workflow icon.
 * @see https://lucide.dev/icons/workflow
 */
export default iconWithClassName(WorkflowIconBase);
