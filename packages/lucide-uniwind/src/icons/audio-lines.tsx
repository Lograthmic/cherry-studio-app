import { AudioLines as AudioLinesIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * AudioLines icon.
 * @see https://lucide.dev/icons/audio-lines
 */
export default iconWithClassName(AudioLinesIconBase);
