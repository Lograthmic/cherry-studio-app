import { Guitar as GuitarIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Guitar icon.
 * @see https://lucide.dev/icons/guitar
 */
export default iconWithClassName(GuitarIconBase);
