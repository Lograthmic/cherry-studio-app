import { Shrink as ShrinkIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Shrink icon.
 * @see https://lucide.dev/icons/shrink
 */
export default iconWithClassName(ShrinkIconBase);
