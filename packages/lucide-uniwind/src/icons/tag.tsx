import { Tag as TagIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Tag icon.
 * @see https://lucide.dev/icons/tag
 */
export default iconWithClassName(TagIconBase);
