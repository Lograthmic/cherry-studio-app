import { MonitorOff as MonitorOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MonitorOff icon.
 * @see https://lucide.dev/icons/monitor-off
 */
export default iconWithClassName(MonitorOffIconBase);
