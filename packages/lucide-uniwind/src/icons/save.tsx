import { Save as SaveIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Save icon.
 * @see https://lucide.dev/icons/save
 */
export default iconWithClassName(SaveIconBase);
