import { Spotlight as SpotlightIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Spotlight icon.
 * @see https://lucide.dev/icons/spotlight
 */
export default iconWithClassName(SpotlightIconBase);
