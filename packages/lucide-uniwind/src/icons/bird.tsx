import { Bird as BirdIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bird icon.
 * @see https://lucide.dev/icons/bird
 */
export default iconWithClassName(BirdIconBase);
