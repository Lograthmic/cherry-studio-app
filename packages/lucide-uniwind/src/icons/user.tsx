import { User as UserIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * User icon.
 * @see https://lucide.dev/icons/user
 */
export default iconWithClassName(UserIconBase);
