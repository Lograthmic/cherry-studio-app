import { Gem as GemIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Gem icon.
 * @see https://lucide.dev/icons/gem
 */
export default iconWithClassName(GemIconBase);
