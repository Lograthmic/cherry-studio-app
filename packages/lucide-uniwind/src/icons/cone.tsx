import { Cone as ConeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cone icon.
 * @see https://lucide.dev/icons/cone
 */
export default iconWithClassName(ConeIconBase);
