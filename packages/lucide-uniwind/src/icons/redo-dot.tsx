import { RedoDot as RedoDotIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * RedoDot icon.
 * @see https://lucide.dev/icons/redo-dot
 */
export default iconWithClassName(RedoDotIconBase);
