import { Beaker as BeakerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Beaker icon.
 * @see https://lucide.dev/icons/beaker
 */
export default iconWithClassName(BeakerIconBase);
