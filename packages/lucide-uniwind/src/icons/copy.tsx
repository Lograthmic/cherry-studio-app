import { Copy as CopyIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Copy icon.
 * @see https://lucide.dev/icons/copy
 */
export default iconWithClassName(CopyIconBase);
