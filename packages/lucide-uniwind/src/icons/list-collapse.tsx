import { ListCollapse as ListCollapseIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListCollapse icon.
 * @see https://lucide.dev/icons/list-collapse
 */
export default iconWithClassName(ListCollapseIconBase);
