import { Calendar as CalendarIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Calendar icon.
 * @see https://lucide.dev/icons/calendar
 */
export default iconWithClassName(CalendarIconBase);
