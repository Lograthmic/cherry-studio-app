import { SquareDashed as SquareDashedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SquareDashed icon.
 * @see https://lucide.dev/icons/square-dashed
 */
export default iconWithClassName(SquareDashedIconBase);
