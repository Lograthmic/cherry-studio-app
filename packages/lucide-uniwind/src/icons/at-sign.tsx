import { AtSign as AtSignIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * AtSign icon.
 * @see https://lucide.dev/icons/at-sign
 */
export default iconWithClassName(AtSignIconBase);
