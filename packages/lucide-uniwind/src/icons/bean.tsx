import { Bean as BeanIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bean icon.
 * @see https://lucide.dev/icons/bean
 */
export default iconWithClassName(BeanIconBase);
