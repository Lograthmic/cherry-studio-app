import { Award as AwardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Award icon.
 * @see https://lucide.dev/icons/award
 */
export default iconWithClassName(AwardIconBase);
