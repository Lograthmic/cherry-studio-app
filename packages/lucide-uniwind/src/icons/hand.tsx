import { Hand as HandIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Hand icon.
 * @see https://lucide.dev/icons/hand
 */
export default iconWithClassName(HandIconBase);
