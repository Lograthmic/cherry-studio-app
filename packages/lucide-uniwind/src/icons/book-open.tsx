import { BookOpen as BookOpenIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookOpen icon.
 * @see https://lucide.dev/icons/book-open
 */
export default iconWithClassName(BookOpenIconBase);
