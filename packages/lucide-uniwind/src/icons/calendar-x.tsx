import { CalendarX as CalendarXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CalendarX icon.
 * @see https://lucide.dev/icons/calendar-x
 */
export default iconWithClassName(CalendarXIconBase);
