import { Backpack as BackpackIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Backpack icon.
 * @see https://lucide.dev/icons/backpack
 */
export default iconWithClassName(BackpackIconBase);
