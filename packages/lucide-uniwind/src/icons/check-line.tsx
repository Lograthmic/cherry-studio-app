import { CheckLine as CheckLineIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CheckLine icon.
 * @see https://lucide.dev/icons/check-line
 */
export default iconWithClassName(CheckLineIconBase);
