import { FileJson as FileJsonIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileJson icon.
 * @see https://lucide.dev/icons/file-json
 */
export default iconWithClassName(FileJsonIconBase);
