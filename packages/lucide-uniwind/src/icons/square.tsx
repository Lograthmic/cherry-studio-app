import { Square as SquareIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Square icon.
 * @see https://lucide.dev/icons/square
 */
export default iconWithClassName(SquareIconBase);
