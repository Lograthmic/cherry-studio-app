import { Fence as FenceIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Fence icon.
 * @see https://lucide.dev/icons/fence
 */
export default iconWithClassName(FenceIconBase);
