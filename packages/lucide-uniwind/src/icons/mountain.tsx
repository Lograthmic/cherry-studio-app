import { Mountain as MountainIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Mountain icon.
 * @see https://lucide.dev/icons/mountain
 */
export default iconWithClassName(MountainIconBase);
