import { MapPinned as MapPinnedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MapPinned icon.
 * @see https://lucide.dev/icons/map-pinned
 */
export default iconWithClassName(MapPinnedIconBase);
