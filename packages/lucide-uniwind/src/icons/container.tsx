import { Container as ContainerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Container icon.
 * @see https://lucide.dev/icons/container
 */
export default iconWithClassName(ContainerIconBase);
