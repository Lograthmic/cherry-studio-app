import { Mail as MailIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Mail icon.
 * @see https://lucide.dev/icons/mail
 */
export default iconWithClassName(MailIconBase);
