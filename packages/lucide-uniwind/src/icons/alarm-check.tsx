import { AlarmCheck as AlarmCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * AlarmCheck icon.
 * @see https://lucide.dev/icons/alarm-check
 */
export default iconWithClassName(AlarmCheckIconBase);
