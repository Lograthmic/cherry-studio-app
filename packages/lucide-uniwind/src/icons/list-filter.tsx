import { ListFilter as ListFilterIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListFilter icon.
 * @see https://lucide.dev/icons/list-filter
 */
export default iconWithClassName(ListFilterIconBase);
