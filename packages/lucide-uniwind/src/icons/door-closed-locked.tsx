import { DoorClosedLocked as DoorClosedLockedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * DoorClosedLocked icon.
 * @see https://lucide.dev/icons/door-closed-locked
 */
export default iconWithClassName(DoorClosedLockedIconBase);
