import { FileLock as FileLockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileLock icon.
 * @see https://lucide.dev/icons/file-lock
 */
export default iconWithClassName(FileLockIconBase);
