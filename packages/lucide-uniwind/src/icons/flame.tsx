import { Flame as FlameIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Flame icon.
 * @see https://lucide.dev/icons/flame
 */
export default iconWithClassName(FlameIconBase);
