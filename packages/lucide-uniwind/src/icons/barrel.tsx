import { Barrel as BarrelIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Barrel icon.
 * @see https://lucide.dev/icons/barrel
 */
export default iconWithClassName(BarrelIconBase);
