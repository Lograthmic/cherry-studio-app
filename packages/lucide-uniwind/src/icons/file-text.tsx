import { FileText as FileTextIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileText icon.
 * @see https://lucide.dev/icons/file-text
 */
export default iconWithClassName(FileTextIconBase);
