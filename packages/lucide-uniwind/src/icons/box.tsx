import { Box as BoxIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Box icon.
 * @see https://lucide.dev/icons/box
 */
export default iconWithClassName(BoxIconBase);
