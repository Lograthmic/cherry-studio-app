import { ImagePlay as ImagePlayIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ImagePlay icon.
 * @see https://lucide.dev/icons/image-play
 */
export default iconWithClassName(ImagePlayIconBase);
