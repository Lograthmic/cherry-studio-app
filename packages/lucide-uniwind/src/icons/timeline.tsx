import { Timeline as TimelineIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Timeline icon.
 * @see https://lucide.dev/icons/timeline
 */
export default iconWithClassName(TimelineIconBase);
