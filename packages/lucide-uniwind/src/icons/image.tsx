import { Image as ImageIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Image icon.
 * @see https://lucide.dev/icons/image
 */
export default iconWithClassName(ImageIconBase);
