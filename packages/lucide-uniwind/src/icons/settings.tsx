import { Settings as SettingsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Settings icon.
 * @see https://lucide.dev/icons/settings
 */
export default iconWithClassName(SettingsIconBase);
