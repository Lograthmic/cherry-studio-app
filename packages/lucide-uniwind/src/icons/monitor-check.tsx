import { MonitorCheck as MonitorCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MonitorCheck icon.
 * @see https://lucide.dev/icons/monitor-check
 */
export default iconWithClassName(MonitorCheckIconBase);
