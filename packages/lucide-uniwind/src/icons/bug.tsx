import { Bug as BugIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bug icon.
 * @see https://lucide.dev/icons/bug
 */
export default iconWithClassName(BugIconBase);
