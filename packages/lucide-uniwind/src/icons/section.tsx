import { Section as SectionIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Section icon.
 * @see https://lucide.dev/icons/section
 */
export default iconWithClassName(SectionIconBase);
