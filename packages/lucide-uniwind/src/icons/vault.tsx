import { Vault as VaultIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Vault icon.
 * @see https://lucide.dev/icons/vault
 */
export default iconWithClassName(VaultIconBase);
