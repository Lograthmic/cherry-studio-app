import { FolderDown as FolderDownIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderDown icon.
 * @see https://lucide.dev/icons/folder-down
 */
export default iconWithClassName(FolderDownIconBase);
