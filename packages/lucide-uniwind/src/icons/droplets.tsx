import { Droplets as DropletsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Droplets icon.
 * @see https://lucide.dev/icons/droplets
 */
export default iconWithClassName(DropletsIconBase);
