import { Binary as BinaryIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Binary icon.
 * @see https://lucide.dev/icons/binary
 */
export default iconWithClassName(BinaryIconBase);
