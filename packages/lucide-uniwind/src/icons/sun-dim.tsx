import { SunDim as SunDimIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SunDim icon.
 * @see https://lucide.dev/icons/sun-dim
 */
export default iconWithClassName(SunDimIconBase);
