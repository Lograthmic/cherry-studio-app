import { FilePlay as FilePlayIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FilePlay icon.
 * @see https://lucide.dev/icons/file-play
 */
export default iconWithClassName(FilePlayIconBase);
