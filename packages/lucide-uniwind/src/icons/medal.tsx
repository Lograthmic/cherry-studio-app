import { Medal as MedalIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Medal icon.
 * @see https://lucide.dev/icons/medal
 */
export default iconWithClassName(MedalIconBase);
