import { Terminal as TerminalIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Terminal icon.
 * @see https://lucide.dev/icons/terminal
 */
export default iconWithClassName(TerminalIconBase);
