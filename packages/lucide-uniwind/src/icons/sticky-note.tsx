import { StickyNote as StickyNoteIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * StickyNote icon.
 * @see https://lucide.dev/icons/sticky-note
 */
export default iconWithClassName(StickyNoteIconBase);
