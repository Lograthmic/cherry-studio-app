import { WifiSync as WifiSyncIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * WifiSync icon.
 * @see https://lucide.dev/icons/wifi-sync
 */
export default iconWithClassName(WifiSyncIconBase);
