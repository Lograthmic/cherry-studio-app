import { SortDesc as SortDescIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SortDesc icon.
 * @see https://lucide.dev/icons/sort-desc
 */
export default iconWithClassName(SortDescIconBase);
