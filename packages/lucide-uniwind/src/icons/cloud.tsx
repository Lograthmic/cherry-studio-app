import { Cloud as CloudIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cloud icon.
 * @see https://lucide.dev/icons/cloud
 */
export default iconWithClassName(CloudIconBase);
