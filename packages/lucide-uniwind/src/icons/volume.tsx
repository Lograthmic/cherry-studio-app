import { Volume as VolumeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Volume icon.
 * @see https://lucide.dev/icons/volume
 */
export default iconWithClassName(VolumeIconBase);
