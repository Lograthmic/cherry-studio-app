import { CloudSync as CloudSyncIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CloudSync icon.
 * @see https://lucide.dev/icons/cloud-sync
 */
export default iconWithClassName(CloudSyncIconBase);
