import { Signpost as SignpostIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Signpost icon.
 * @see https://lucide.dev/icons/signpost
 */
export default iconWithClassName(SignpostIconBase);
