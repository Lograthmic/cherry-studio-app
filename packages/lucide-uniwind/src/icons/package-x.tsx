import { PackageX as PackageXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PackageX icon.
 * @see https://lucide.dev/icons/package-x
 */
export default iconWithClassName(PackageXIconBase);
