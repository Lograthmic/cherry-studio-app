import { Hotel as HotelIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Hotel icon.
 * @see https://lucide.dev/icons/hotel
 */
export default iconWithClassName(HotelIconBase);
