import { Hash as HashIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Hash icon.
 * @see https://lucide.dev/icons/hash
 */
export default iconWithClassName(HashIconBase);
