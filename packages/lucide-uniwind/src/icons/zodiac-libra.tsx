import { ZodiacLibra as ZodiacLibraIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ZodiacLibra icon.
 * @see https://lucide.dev/icons/zodiac-libra
 */
export default iconWithClassName(ZodiacLibraIconBase);
