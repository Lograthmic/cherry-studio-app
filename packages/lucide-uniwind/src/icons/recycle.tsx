import { Recycle as RecycleIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Recycle icon.
 * @see https://lucide.dev/icons/recycle
 */
export default iconWithClassName(RecycleIconBase);
