import { Undo as UndoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Undo icon.
 * @see https://lucide.dev/icons/undo
 */
export default iconWithClassName(UndoIconBase);
