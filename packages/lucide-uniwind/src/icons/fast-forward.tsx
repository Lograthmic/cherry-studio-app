import { FastForward as FastForwardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FastForward icon.
 * @see https://lucide.dev/icons/fast-forward
 */
export default iconWithClassName(FastForwardIconBase);
