import { BookImage as BookImageIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookImage icon.
 * @see https://lucide.dev/icons/book-image
 */
export default iconWithClassName(BookImageIconBase);
