import { FolderClock as FolderClockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderClock icon.
 * @see https://lucide.dev/icons/folder-clock
 */
export default iconWithClassName(FolderClockIconBase);
