import { SkipBack as SkipBackIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SkipBack icon.
 * @see https://lucide.dev/icons/skip-back
 */
export default iconWithClassName(SkipBackIconBase);
