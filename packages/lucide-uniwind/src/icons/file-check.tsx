import { FileCheck as FileCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileCheck icon.
 * @see https://lucide.dev/icons/file-check
 */
export default iconWithClassName(FileCheckIconBase);
