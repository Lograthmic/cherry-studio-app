import { Target as TargetIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Target icon.
 * @see https://lucide.dev/icons/target
 */
export default iconWithClassName(TargetIconBase);
