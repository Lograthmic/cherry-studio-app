import { Piano as PianoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Piano icon.
 * @see https://lucide.dev/icons/piano
 */
export default iconWithClassName(PianoIconBase);
