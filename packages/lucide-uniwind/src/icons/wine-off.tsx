import { WineOff as WineOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * WineOff icon.
 * @see https://lucide.dev/icons/wine-off
 */
export default iconWithClassName(WineOffIconBase);
