import { PhoneOff as PhoneOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PhoneOff icon.
 * @see https://lucide.dev/icons/phone-off
 */
export default iconWithClassName(PhoneOffIconBase);
