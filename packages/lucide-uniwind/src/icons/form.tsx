import { Form as FormIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Form icon.
 * @see https://lucide.dev/icons/form
 */
export default iconWithClassName(FormIconBase);
