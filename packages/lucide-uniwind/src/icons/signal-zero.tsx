import { SignalZero as SignalZeroIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SignalZero icon.
 * @see https://lucide.dev/icons/signal-zero
 */
export default iconWithClassName(SignalZeroIconBase);
