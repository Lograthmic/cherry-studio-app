import { CloudUpload as CloudUploadIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CloudUpload icon.
 * @see https://lucide.dev/icons/cloud-upload
 */
export default iconWithClassName(CloudUploadIconBase);
