import { ReplaceAll as ReplaceAllIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ReplaceAll icon.
 * @see https://lucide.dev/icons/replace-all
 */
export default iconWithClassName(ReplaceAllIconBase);
