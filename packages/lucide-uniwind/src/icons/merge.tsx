import { Merge as MergeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Merge icon.
 * @see https://lucide.dev/icons/merge
 */
export default iconWithClassName(MergeIconBase);
