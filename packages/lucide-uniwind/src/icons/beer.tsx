import { Beer as BeerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Beer icon.
 * @see https://lucide.dev/icons/beer
 */
export default iconWithClassName(BeerIconBase);
