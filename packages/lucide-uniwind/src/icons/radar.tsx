import { Radar as RadarIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Radar icon.
 * @see https://lucide.dev/icons/radar
 */
export default iconWithClassName(RadarIconBase);
