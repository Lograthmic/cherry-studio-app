import { Bitcoin as BitcoinIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bitcoin icon.
 * @see https://lucide.dev/icons/bitcoin
 */
export default iconWithClassName(BitcoinIconBase);
