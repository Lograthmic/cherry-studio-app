import { Rainbow as RainbowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Rainbow icon.
 * @see https://lucide.dev/icons/rainbow
 */
export default iconWithClassName(RainbowIconBase);
