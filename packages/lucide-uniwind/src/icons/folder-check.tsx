import { FolderCheck as FolderCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderCheck icon.
 * @see https://lucide.dev/icons/folder-check
 */
export default iconWithClassName(FolderCheckIconBase);
