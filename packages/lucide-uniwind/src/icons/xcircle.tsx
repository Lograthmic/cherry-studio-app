import { XCircle as XCircleIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * XCircle icon.
 * @see https://lucide.dev/icons/xcircle
 */
export default iconWithClassName(XCircleIconBase);
