import { Slice as SliceIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Slice icon.
 * @see https://lucide.dev/icons/slice
 */
export default iconWithClassName(SliceIconBase);
