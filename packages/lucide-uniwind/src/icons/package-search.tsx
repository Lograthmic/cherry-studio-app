import { PackageSearch as PackageSearchIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PackageSearch icon.
 * @see https://lucide.dev/icons/package-search
 */
export default iconWithClassName(PackageSearchIconBase);
