import { Palette as PaletteIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Palette icon.
 * @see https://lucide.dev/icons/palette
 */
export default iconWithClassName(PaletteIconBase);
