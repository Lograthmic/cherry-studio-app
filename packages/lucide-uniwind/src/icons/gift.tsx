import { Gift as GiftIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Gift icon.
 * @see https://lucide.dev/icons/gift
 */
export default iconWithClassName(GiftIconBase);
