import { Gpu as GpuIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Gpu icon.
 * @see https://lucide.dev/icons/gpu
 */
export default iconWithClassName(GpuIconBase);
