import { Heart as HeartIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Heart icon.
 * @see https://lucide.dev/icons/heart
 */
export default iconWithClassName(HeartIconBase);
