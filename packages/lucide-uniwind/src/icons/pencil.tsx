import { Pencil as PencilIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pencil icon.
 * @see https://lucide.dev/icons/pencil
 */
export default iconWithClassName(PencilIconBase);
