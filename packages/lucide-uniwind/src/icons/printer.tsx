import { Printer as PrinterIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Printer icon.
 * @see https://lucide.dev/icons/printer
 */
export default iconWithClassName(PrinterIconBase);
