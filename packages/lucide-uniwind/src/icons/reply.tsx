import { Reply as ReplyIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Reply icon.
 * @see https://lucide.dev/icons/reply
 */
export default iconWithClassName(ReplyIconBase);
