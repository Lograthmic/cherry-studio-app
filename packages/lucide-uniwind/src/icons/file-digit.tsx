import { FileDigit as FileDigitIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileDigit icon.
 * @see https://lucide.dev/icons/file-digit
 */
export default iconWithClassName(FileDigitIconBase);
