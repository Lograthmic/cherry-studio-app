import { Split as SplitIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Split icon.
 * @see https://lucide.dev/icons/split
 */
export default iconWithClassName(SplitIconBase);
