import { BatteryWarning as BatteryWarningIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BatteryWarning icon.
 * @see https://lucide.dev/icons/battery-warning
 */
export default iconWithClassName(BatteryWarningIconBase);
