import { Ticket as TicketIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Ticket icon.
 * @see https://lucide.dev/icons/ticket
 */
export default iconWithClassName(TicketIconBase);
