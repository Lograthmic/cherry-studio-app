import { Coins as CoinsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Coins icon.
 * @see https://lucide.dev/icons/coins
 */
export default iconWithClassName(CoinsIconBase);
