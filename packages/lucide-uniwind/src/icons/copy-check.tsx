import { CopyCheck as CopyCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CopyCheck icon.
 * @see https://lucide.dev/icons/copy-check
 */
export default iconWithClassName(CopyCheckIconBase);
