import { ServerOff as ServerOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ServerOff icon.
 * @see https://lucide.dev/icons/server-off
 */
export default iconWithClassName(ServerOffIconBase);
