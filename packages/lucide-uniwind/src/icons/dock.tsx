import { Dock as DockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Dock icon.
 * @see https://lucide.dev/icons/dock
 */
export default iconWithClassName(DockIconBase);
