import { MonitorCog as MonitorCogIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MonitorCog icon.
 * @see https://lucide.dev/icons/monitor-cog
 */
export default iconWithClassName(MonitorCogIconBase);
