import { CloudCheck as CloudCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CloudCheck icon.
 * @see https://lucide.dev/icons/cloud-check
 */
export default iconWithClassName(CloudCheckIconBase);
