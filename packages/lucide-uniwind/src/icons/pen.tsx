import { Pen as PenIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pen icon.
 * @see https://lucide.dev/icons/pen
 */
export default iconWithClassName(PenIconBase);
