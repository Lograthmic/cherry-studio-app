import { Blinds as BlindsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Blinds icon.
 * @see https://lucide.dev/icons/blinds
 */
export default iconWithClassName(BlindsIconBase);
