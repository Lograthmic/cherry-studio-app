import { Flower as FlowerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Flower icon.
 * @see https://lucide.dev/icons/flower
 */
export default iconWithClassName(FlowerIconBase);
