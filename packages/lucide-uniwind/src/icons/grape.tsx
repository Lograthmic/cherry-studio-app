import { Grape as GrapeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Grape icon.
 * @see https://lucide.dev/icons/grape
 */
export default iconWithClassName(GrapeIconBase);
