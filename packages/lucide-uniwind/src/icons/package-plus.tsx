import { PackagePlus as PackagePlusIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PackagePlus icon.
 * @see https://lucide.dev/icons/package-plus
 */
export default iconWithClassName(PackagePlusIconBase);
