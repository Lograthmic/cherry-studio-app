import { ListTodo as ListTodoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListTodo icon.
 * @see https://lucide.dev/icons/list-todo
 */
export default iconWithClassName(ListTodoIconBase);
