import { Egg as EggIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Egg icon.
 * @see https://lucide.dev/icons/egg
 */
export default iconWithClassName(EggIconBase);
