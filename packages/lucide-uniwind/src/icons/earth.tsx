import { Earth as EarthIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Earth icon.
 * @see https://lucide.dev/icons/earth
 */
export default iconWithClassName(EarthIconBase);
