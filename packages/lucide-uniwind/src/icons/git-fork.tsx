import { GitFork as GitForkIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * GitFork icon.
 * @see https://lucide.dev/icons/git-fork
 */
export default iconWithClassName(GitForkIconBase);
