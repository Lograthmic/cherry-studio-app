import { FolderLock as FolderLockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderLock icon.
 * @see https://lucide.dev/icons/folder-lock
 */
export default iconWithClassName(FolderLockIconBase);
