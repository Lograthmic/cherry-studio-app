import { EyeClosed as EyeClosedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * EyeClosed icon.
 * @see https://lucide.dev/icons/eye-closed
 */
export default iconWithClassName(EyeClosedIconBase);
