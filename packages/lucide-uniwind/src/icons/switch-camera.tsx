import { SwitchCamera as SwitchCameraIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SwitchCamera icon.
 * @see https://lucide.dev/icons/switch-camera
 */
export default iconWithClassName(SwitchCameraIconBase);
