import { LocateFixed as LocateFixedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * LocateFixed icon.
 * @see https://lucide.dev/icons/locate-fixed
 */
export default iconWithClassName(LocateFixedIconBase);
