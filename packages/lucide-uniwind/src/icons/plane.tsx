import { Plane as PlaneIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Plane icon.
 * @see https://lucide.dev/icons/plane
 */
export default iconWithClassName(PlaneIconBase);
