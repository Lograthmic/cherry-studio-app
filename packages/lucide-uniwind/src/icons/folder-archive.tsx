import { FolderArchive as FolderArchiveIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderArchive icon.
 * @see https://lucide.dev/icons/folder-archive
 */
export default iconWithClassName(FolderArchiveIconBase);
