import { Cherry as CherryIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cherry icon.
 * @see https://lucide.dev/icons/cherry
 */
export default iconWithClassName(CherryIconBase);
