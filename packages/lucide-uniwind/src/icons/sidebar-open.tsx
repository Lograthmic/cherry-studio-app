import { SidebarOpen as SidebarOpenIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SidebarOpen icon.
 * @see https://lucide.dev/icons/sidebar-open
 */
export default iconWithClassName(SidebarOpenIconBase);
