import { FilePen as FilePenIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FilePen icon.
 * @see https://lucide.dev/icons/file-pen
 */
export default iconWithClassName(FilePenIconBase);
