import { Leaf as LeafIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Leaf icon.
 * @see https://lucide.dev/icons/leaf
 */
export default iconWithClassName(LeafIconBase);
