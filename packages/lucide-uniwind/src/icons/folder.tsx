import { Folder as FolderIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Folder icon.
 * @see https://lucide.dev/icons/folder
 */
export default iconWithClassName(FolderIconBase);
