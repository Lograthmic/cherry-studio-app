import { WrapText as WrapTextIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * WrapText icon.
 * @see https://lucide.dev/icons/wrap-text
 */
export default iconWithClassName(WrapTextIconBase);
