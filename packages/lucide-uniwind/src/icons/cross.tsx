import { Cross as CrossIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cross icon.
 * @see https://lucide.dev/icons/cross
 */
export default iconWithClassName(CrossIconBase);
