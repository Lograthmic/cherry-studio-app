import { CaseUpper as CaseUpperIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CaseUpper icon.
 * @see https://lucide.dev/icons/case-upper
 */
export default iconWithClassName(CaseUpperIconBase);
