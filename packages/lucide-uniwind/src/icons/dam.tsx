import { Dam as DamIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Dam icon.
 * @see https://lucide.dev/icons/dam
 */
export default iconWithClassName(DamIconBase);
