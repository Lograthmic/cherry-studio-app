import { Vote as VoteIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Vote icon.
 * @see https://lucide.dev/icons/vote
 */
export default iconWithClassName(VoteIconBase);
