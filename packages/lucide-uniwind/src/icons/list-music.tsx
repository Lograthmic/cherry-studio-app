import { ListMusic as ListMusicIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListMusic icon.
 * @see https://lucide.dev/icons/list-music
 */
export default iconWithClassName(ListMusicIconBase);
