import { MonitorPlay as MonitorPlayIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MonitorPlay icon.
 * @see https://lucide.dev/icons/monitor-play
 */
export default iconWithClassName(MonitorPlayIconBase);
