import { Brain as BrainIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Brain icon.
 * @see https://lucide.dev/icons/brain
 */
export default iconWithClassName(BrainIconBase);
