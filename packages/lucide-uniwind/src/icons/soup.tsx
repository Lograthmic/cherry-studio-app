import { Soup as SoupIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Soup icon.
 * @see https://lucide.dev/icons/soup
 */
export default iconWithClassName(SoupIconBase);
