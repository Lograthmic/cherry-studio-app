import { Speaker as SpeakerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Speaker icon.
 * @see https://lucide.dev/icons/speaker
 */
export default iconWithClassName(SpeakerIconBase);
