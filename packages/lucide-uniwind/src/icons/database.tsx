import { Database as DatabaseIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Database icon.
 * @see https://lucide.dev/icons/database
 */
export default iconWithClassName(DatabaseIconBase);
