import { PaintBucket as PaintBucketIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PaintBucket icon.
 * @see https://lucide.dev/icons/paint-bucket
 */
export default iconWithClassName(PaintBucketIconBase);
