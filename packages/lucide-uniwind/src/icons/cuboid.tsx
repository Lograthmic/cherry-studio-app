import { Cuboid as CuboidIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cuboid icon.
 * @see https://lucide.dev/icons/cuboid
 */
export default iconWithClassName(CuboidIconBase);
