import { ClipboardCheck as ClipboardCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ClipboardCheck icon.
 * @see https://lucide.dev/icons/clipboard-check
 */
export default iconWithClassName(ClipboardCheckIconBase);
