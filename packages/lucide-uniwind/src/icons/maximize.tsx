import { Maximize as MaximizeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Maximize icon.
 * @see https://lucide.dev/icons/maximize
 */
export default iconWithClassName(MaximizeIconBase);
