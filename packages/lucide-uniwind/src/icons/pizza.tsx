import { Pizza as PizzaIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pizza icon.
 * @see https://lucide.dev/icons/pizza
 */
export default iconWithClassName(PizzaIconBase);
