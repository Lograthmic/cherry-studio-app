import { Crop as CropIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Crop icon.
 * @see https://lucide.dev/icons/crop
 */
export default iconWithClassName(CropIconBase);
