import { Store as StoreIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Store icon.
 * @see https://lucide.dev/icons/store
 */
export default iconWithClassName(StoreIconBase);
