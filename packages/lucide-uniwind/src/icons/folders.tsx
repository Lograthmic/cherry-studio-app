import { Folders as FoldersIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Folders icon.
 * @see https://lucide.dev/icons/folders
 */
export default iconWithClassName(FoldersIconBase);
