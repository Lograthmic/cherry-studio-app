import { Keyboard as KeyboardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Keyboard icon.
 * @see https://lucide.dev/icons/keyboard
 */
export default iconWithClassName(KeyboardIconBase);
