import { BookCheck as BookCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookCheck icon.
 * @see https://lucide.dev/icons/book-check
 */
export default iconWithClassName(BookCheckIconBase);
