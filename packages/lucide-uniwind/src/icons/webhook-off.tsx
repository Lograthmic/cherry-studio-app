import { WebhookOff as WebhookOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * WebhookOff icon.
 * @see https://lucide.dev/icons/webhook-off
 */
export default iconWithClassName(WebhookOffIconBase);
