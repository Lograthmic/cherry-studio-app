import { SunSnow as SunSnowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SunSnow icon.
 * @see https://lucide.dev/icons/sun-snow
 */
export default iconWithClassName(SunSnowIconBase);
