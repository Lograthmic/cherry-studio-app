import { Shell as ShellIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Shell icon.
 * @see https://lucide.dev/icons/shell
 */
export default iconWithClassName(ShellIconBase);
