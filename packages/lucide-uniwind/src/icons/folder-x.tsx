import { FolderX as FolderXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderX icon.
 * @see https://lucide.dev/icons/folder-x
 */
export default iconWithClassName(FolderXIconBase);
