import { Ship as ShipIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Ship icon.
 * @see https://lucide.dev/icons/ship
 */
export default iconWithClassName(ShipIconBase);
