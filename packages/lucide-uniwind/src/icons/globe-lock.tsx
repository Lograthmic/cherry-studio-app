import { GlobeLock as GlobeLockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * GlobeLock icon.
 * @see https://lucide.dev/icons/globe-lock
 */
export default iconWithClassName(GlobeLockIconBase);
