import { FileImage as FileImageIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileImage icon.
 * @see https://lucide.dev/icons/file-image
 */
export default iconWithClassName(FileImageIconBase);
