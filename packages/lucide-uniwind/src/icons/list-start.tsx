import { ListStart as ListStartIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListStart icon.
 * @see https://lucide.dev/icons/list-start
 */
export default iconWithClassName(ListStartIconBase);
