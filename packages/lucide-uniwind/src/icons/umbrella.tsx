import { Umbrella as UmbrellaIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Umbrella icon.
 * @see https://lucide.dev/icons/umbrella
 */
export default iconWithClassName(UmbrellaIconBase);
