import { Van as VanIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Van icon.
 * @see https://lucide.dev/icons/van
 */
export default iconWithClassName(VanIconBase);
