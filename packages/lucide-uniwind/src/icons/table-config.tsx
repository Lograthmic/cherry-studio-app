import { TableConfig as TableConfigIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TableConfig icon.
 * @see https://lucide.dev/icons/table-config
 */
export default iconWithClassName(TableConfigIconBase);
