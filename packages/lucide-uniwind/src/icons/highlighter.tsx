import { Highlighter as HighlighterIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Highlighter icon.
 * @see https://lucide.dev/icons/highlighter
 */
export default iconWithClassName(HighlighterIconBase);
