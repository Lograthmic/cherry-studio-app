import { Redo as RedoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Redo icon.
 * @see https://lucide.dev/icons/redo
 */
export default iconWithClassName(RedoIconBase);
