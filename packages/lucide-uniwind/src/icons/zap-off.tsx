import { ZapOff as ZapOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ZapOff icon.
 * @see https://lucide.dev/icons/zap-off
 */
export default iconWithClassName(ZapOffIconBase);
