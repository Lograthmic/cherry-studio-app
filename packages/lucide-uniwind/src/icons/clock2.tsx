import { Clock2 as Clock2IconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Clock2 icon.
 * @see https://lucide.dev/icons/clock2
 */
export default iconWithClassName(Clock2IconBase);
