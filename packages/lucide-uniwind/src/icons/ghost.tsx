import { Ghost as GhostIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Ghost icon.
 * @see https://lucide.dev/icons/ghost
 */
export default iconWithClassName(GhostIconBase);
