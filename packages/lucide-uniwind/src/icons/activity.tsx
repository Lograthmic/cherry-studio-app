import { Activity as ActivityIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Activity icon.
 * @see https://lucide.dev/icons/activity
 */
export default iconWithClassName(ActivityIconBase);
