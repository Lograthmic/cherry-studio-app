import { VolumeOff as VolumeOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * VolumeOff icon.
 * @see https://lucide.dev/icons/volume-off
 */
export default iconWithClassName(VolumeOffIconBase);
