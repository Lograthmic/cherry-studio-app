import { KeyRound as KeyRoundIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * KeyRound icon.
 * @see https://lucide.dev/icons/key-round
 */
export default iconWithClassName(KeyRoundIconBase);
