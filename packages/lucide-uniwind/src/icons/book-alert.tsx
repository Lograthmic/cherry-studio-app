import { BookAlert as BookAlertIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookAlert icon.
 * @see https://lucide.dev/icons/book-alert
 */
export default iconWithClassName(BookAlertIconBase);
