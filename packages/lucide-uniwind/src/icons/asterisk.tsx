import { Asterisk as AsteriskIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Asterisk icon.
 * @see https://lucide.dev/icons/asterisk
 */
export default iconWithClassName(AsteriskIconBase);
