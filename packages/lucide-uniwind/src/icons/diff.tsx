import { Diff as DiffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Diff icon.
 * @see https://lucide.dev/icons/diff
 */
export default iconWithClassName(DiffIconBase);
