import { Map as MapIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Map icon.
 * @see https://lucide.dev/icons/map
 */
export default iconWithClassName(MapIconBase);
