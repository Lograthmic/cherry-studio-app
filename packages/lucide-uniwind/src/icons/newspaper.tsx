import { Newspaper as NewspaperIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Newspaper icon.
 * @see https://lucide.dev/icons/newspaper
 */
export default iconWithClassName(NewspaperIconBase);
