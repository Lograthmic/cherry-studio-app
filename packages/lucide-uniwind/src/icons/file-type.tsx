import { FileType as FileTypeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileType icon.
 * @see https://lucide.dev/icons/file-type
 */
export default iconWithClassName(FileTypeIconBase);
