import { Bus as BusIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bus icon.
 * @see https://lucide.dev/icons/bus
 */
export default iconWithClassName(BusIconBase);
