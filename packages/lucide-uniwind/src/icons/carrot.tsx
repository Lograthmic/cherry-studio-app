import { Carrot as CarrotIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Carrot icon.
 * @see https://lucide.dev/icons/carrot
 */
export default iconWithClassName(CarrotIconBase);
