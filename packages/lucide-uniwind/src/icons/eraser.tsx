import { Eraser as EraserIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Eraser icon.
 * @see https://lucide.dev/icons/eraser
 */
export default iconWithClassName(EraserIconBase);
