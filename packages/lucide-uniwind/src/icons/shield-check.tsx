import { ShieldCheck as ShieldCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ShieldCheck icon.
 * @see https://lucide.dev/icons/shield-check
 */
export default iconWithClassName(ShieldCheckIconBase);
