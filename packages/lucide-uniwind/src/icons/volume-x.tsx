import { VolumeX as VolumeXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * VolumeX icon.
 * @see https://lucide.dev/icons/volume-x
 */
export default iconWithClassName(VolumeXIconBase);
