import { IdCard as IdCardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * IdCard icon.
 * @see https://lucide.dev/icons/id-card
 */
export default iconWithClassName(IdCardIconBase);
