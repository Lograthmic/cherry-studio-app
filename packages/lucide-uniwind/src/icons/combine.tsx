import { Combine as CombineIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Combine icon.
 * @see https://lucide.dev/icons/combine
 */
export default iconWithClassName(CombineIconBase);
