import { CardSim as CardSimIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CardSim icon.
 * @see https://lucide.dev/icons/card-sim
 */
export default iconWithClassName(CardSimIconBase);
