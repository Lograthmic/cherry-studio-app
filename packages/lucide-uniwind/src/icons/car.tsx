import { Car as CarIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Car icon.
 * @see https://lucide.dev/icons/car
 */
export default iconWithClassName(CarIconBase);
