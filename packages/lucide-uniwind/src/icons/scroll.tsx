import { Scroll as ScrollIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Scroll icon.
 * @see https://lucide.dev/icons/scroll
 */
export default iconWithClassName(ScrollIconBase);
