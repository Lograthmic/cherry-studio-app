import { Clipboard as ClipboardIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Clipboard icon.
 * @see https://lucide.dev/icons/clipboard
 */
export default iconWithClassName(ClipboardIconBase);
