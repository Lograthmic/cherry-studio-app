import { Code as CodeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Code icon.
 * @see https://lucide.dev/icons/code
 */
export default iconWithClassName(CodeIconBase);
