import { Contact as ContactIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Contact icon.
 * @see https://lucide.dev/icons/contact
 */
export default iconWithClassName(ContactIconBase);
