import { GitGraph as GitGraphIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * GitGraph icon.
 * @see https://lucide.dev/icons/git-graph
 */
export default iconWithClassName(GitGraphIconBase);
