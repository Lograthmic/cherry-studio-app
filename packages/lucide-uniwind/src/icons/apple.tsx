import { Apple as AppleIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Apple icon.
 * @see https://lucide.dev/icons/apple
 */
export default iconWithClassName(AppleIconBase);
