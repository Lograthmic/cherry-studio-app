import { FolderCode as FolderCodeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderCode icon.
 * @see https://lucide.dev/icons/folder-code
 */
export default iconWithClassName(FolderCodeIconBase);
