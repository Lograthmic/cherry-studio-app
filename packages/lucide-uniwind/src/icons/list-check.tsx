import { ListCheck as ListCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListCheck icon.
 * @see https://lucide.dev/icons/list-check
 */
export default iconWithClassName(ListCheckIconBase);
