import { FileUser as FileUserIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileUser icon.
 * @see https://lucide.dev/icons/file-user
 */
export default iconWithClassName(FileUserIconBase);
