import { Contrast as ContrastIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Contrast icon.
 * @see https://lucide.dev/icons/contrast
 */
export default iconWithClassName(ContrastIconBase);
