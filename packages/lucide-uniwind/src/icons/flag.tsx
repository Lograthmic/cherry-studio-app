import { Flag as FlagIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Flag icon.
 * @see https://lucide.dev/icons/flag
 */
export default iconWithClassName(FlagIconBase);
