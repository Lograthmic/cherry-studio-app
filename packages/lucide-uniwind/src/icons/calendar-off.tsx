import { CalendarOff as CalendarOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CalendarOff icon.
 * @see https://lucide.dev/icons/calendar-off
 */
export default iconWithClassName(CalendarOffIconBase);
