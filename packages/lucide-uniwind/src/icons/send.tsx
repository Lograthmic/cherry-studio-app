import { Send as SendIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Send icon.
 * @see https://lucide.dev/icons/send
 */
export default iconWithClassName(SendIconBase);
