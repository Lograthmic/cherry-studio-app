import { WifiCog as WifiCogIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * WifiCog icon.
 * @see https://lucide.dev/icons/wifi-cog
 */
export default iconWithClassName(WifiCogIconBase);
