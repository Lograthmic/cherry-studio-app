import { Sun as SunIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Sun icon.
 * @see https://lucide.dev/icons/sun
 */
export default iconWithClassName(SunIconBase);
