import { ListX as ListXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListX icon.
 * @see https://lucide.dev/icons/list-x
 */
export default iconWithClassName(ListXIconBase);
