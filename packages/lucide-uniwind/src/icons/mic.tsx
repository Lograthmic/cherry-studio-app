import { Mic as MicIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Mic icon.
 * @see https://lucide.dev/icons/mic
 */
export default iconWithClassName(MicIconBase);
