import { Monitor as MonitorIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Monitor icon.
 * @see https://lucide.dev/icons/monitor
 */
export default iconWithClassName(MonitorIconBase);
