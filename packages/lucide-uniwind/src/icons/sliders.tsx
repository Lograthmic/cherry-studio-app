import { Sliders as SlidersIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Sliders icon.
 * @see https://lucide.dev/icons/sliders
 */
export default iconWithClassName(SlidersIconBase);
