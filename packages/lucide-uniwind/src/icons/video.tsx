import { Video as VideoIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Video icon.
 * @see https://lucide.dev/icons/video
 */
export default iconWithClassName(VideoIconBase);
