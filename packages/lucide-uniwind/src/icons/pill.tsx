import { Pill as PillIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pill icon.
 * @see https://lucide.dev/icons/pill
 */
export default iconWithClassName(PillIconBase);
