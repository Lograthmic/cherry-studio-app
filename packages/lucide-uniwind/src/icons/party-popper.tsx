import { PartyPopper as PartyPopperIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PartyPopper icon.
 * @see https://lucide.dev/icons/party-popper
 */
export default iconWithClassName(PartyPopperIconBase);
