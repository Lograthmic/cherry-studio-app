import { PhoneForwarded as PhoneForwardedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PhoneForwarded icon.
 * @see https://lucide.dev/icons/phone-forwarded
 */
export default iconWithClassName(PhoneForwardedIconBase);
