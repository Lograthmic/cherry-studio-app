import { FolderKey as FolderKeyIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderKey icon.
 * @see https://lucide.dev/icons/folder-key
 */
export default iconWithClassName(FolderKeyIconBase);
