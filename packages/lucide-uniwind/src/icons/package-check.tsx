import { PackageCheck as PackageCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PackageCheck icon.
 * @see https://lucide.dev/icons/package-check
 */
export default iconWithClassName(PackageCheckIconBase);
