import { ClipboardX as ClipboardXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ClipboardX icon.
 * @see https://lucide.dev/icons/clipboard-x
 */
export default iconWithClassName(ClipboardXIconBase);
