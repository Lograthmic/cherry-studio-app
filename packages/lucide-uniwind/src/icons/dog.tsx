import { Dog as DogIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Dog icon.
 * @see https://lucide.dev/icons/dog
 */
export default iconWithClassName(DogIconBase);
