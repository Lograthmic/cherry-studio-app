import { Battery as BatteryIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Battery icon.
 * @see https://lucide.dev/icons/battery
 */
export default iconWithClassName(BatteryIconBase);
