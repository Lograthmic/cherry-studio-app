import { Boxes as BoxesIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Boxes icon.
 * @see https://lucide.dev/icons/boxes
 */
export default iconWithClassName(BoxesIconBase);
