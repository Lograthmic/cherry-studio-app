import { ServerCog as ServerCogIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ServerCog icon.
 * @see https://lucide.dev/icons/server-cog
 */
export default iconWithClassName(ServerCogIconBase);
