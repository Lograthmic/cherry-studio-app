import { Goal as GoalIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Goal icon.
 * @see https://lucide.dev/icons/goal
 */
export default iconWithClassName(GoalIconBase);
