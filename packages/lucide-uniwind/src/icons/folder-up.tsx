import { FolderUp as FolderUpIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderUp icon.
 * @see https://lucide.dev/icons/folder-up
 */
export default iconWithClassName(FolderUpIconBase);
