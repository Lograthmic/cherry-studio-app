import { Locate as LocateIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Locate icon.
 * @see https://lucide.dev/icons/locate
 */
export default iconWithClassName(LocateIconBase);
