import { GitCompare as GitCompareIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * GitCompare icon.
 * @see https://lucide.dev/icons/git-compare
 */
export default iconWithClassName(GitCompareIconBase);
