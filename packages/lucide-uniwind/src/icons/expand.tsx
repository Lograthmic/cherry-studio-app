import { Expand as ExpandIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Expand icon.
 * @see https://lucide.dev/icons/expand
 */
export default iconWithClassName(ExpandIconBase);
