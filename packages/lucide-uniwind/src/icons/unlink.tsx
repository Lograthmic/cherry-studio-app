import { Unlink as UnlinkIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Unlink icon.
 * @see https://lucide.dev/icons/unlink
 */
export default iconWithClassName(UnlinkIconBase);
