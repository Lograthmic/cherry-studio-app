import { SignalHigh as SignalHighIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SignalHigh icon.
 * @see https://lucide.dev/icons/signal-high
 */
export default iconWithClassName(SignalHighIconBase);
