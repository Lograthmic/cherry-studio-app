import { Currency as CurrencyIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Currency icon.
 * @see https://lucide.dev/icons/currency
 */
export default iconWithClassName(CurrencyIconBase);
