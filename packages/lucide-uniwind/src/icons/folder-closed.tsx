import { FolderClosed as FolderClosedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderClosed icon.
 * @see https://lucide.dev/icons/folder-closed
 */
export default iconWithClassName(FolderClosedIconBase);
