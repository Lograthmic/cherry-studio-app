import { Usb as UsbIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Usb icon.
 * @see https://lucide.dev/icons/usb
 */
export default iconWithClassName(UsbIconBase);
