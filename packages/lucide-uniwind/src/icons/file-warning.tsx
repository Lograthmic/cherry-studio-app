import { FileWarning as FileWarningIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileWarning icon.
 * @see https://lucide.dev/icons/file-warning
 */
export default iconWithClassName(FileWarningIconBase);
