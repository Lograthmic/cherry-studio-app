import { NotepadText as NotepadTextIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * NotepadText icon.
 * @see https://lucide.dev/icons/notepad-text
 */
export default iconWithClassName(NotepadTextIconBase);
