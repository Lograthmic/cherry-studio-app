import { AppWindow as AppWindowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * AppWindow icon.
 * @see https://lucide.dev/icons/app-window
 */
export default iconWithClassName(AppWindowIconBase);
