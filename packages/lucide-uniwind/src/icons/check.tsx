import { Check as CheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Check icon.
 * @see https://lucide.dev/icons/check
 */
export default iconWithClassName(CheckIconBase);
