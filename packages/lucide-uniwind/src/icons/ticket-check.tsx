import { TicketCheck as TicketCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TicketCheck icon.
 * @see https://lucide.dev/icons/ticket-check
 */
export default iconWithClassName(TicketCheckIconBase);
