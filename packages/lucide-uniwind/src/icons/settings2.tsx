import { Settings2 as Settings2IconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Settings2 icon.
 * @see https://lucide.dev/icons/settings2
 */
export default iconWithClassName(Settings2IconBase);
