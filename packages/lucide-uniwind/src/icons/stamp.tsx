import { Stamp as StampIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Stamp icon.
 * @see https://lucide.dev/icons/stamp
 */
export default iconWithClassName(StampIconBase);
