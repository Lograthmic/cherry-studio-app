import { PrinterX as PrinterXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * PrinterX icon.
 * @see https://lucide.dev/icons/printer-x
 */
export default iconWithClassName(PrinterXIconBase);
