import { Fan as FanIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Fan icon.
 * @see https://lucide.dev/icons/fan
 */
export default iconWithClassName(FanIconBase);
