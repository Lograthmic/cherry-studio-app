import { ListEnd as ListEndIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListEnd icon.
 * @see https://lucide.dev/icons/list-end
 */
export default iconWithClassName(ListEndIconBase);
