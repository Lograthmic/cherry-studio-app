import { Wrench as WrenchIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Wrench icon.
 * @see https://lucide.dev/icons/wrench
 */
export default iconWithClassName(WrenchIconBase);
