import { BookUser as BookUserIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookUser icon.
 * @see https://lucide.dev/icons/book-user
 */
export default iconWithClassName(BookUserIconBase);
