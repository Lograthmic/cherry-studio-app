import { FileMusic as FileMusicIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileMusic icon.
 * @see https://lucide.dev/icons/file-music
 */
export default iconWithClassName(FileMusicIconBase);
