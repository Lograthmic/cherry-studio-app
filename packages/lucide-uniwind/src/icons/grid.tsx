import { Grid as GridIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Grid icon.
 * @see https://lucide.dev/icons/grid
 */
export default iconWithClassName(GridIconBase);
