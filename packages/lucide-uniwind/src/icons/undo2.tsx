import { Undo2 as Undo2IconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Undo2 icon.
 * @see https://lucide.dev/icons/undo2
 */
export default iconWithClassName(Undo2IconBase);
