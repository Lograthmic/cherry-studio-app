import { Layers as LayersIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Layers icon.
 * @see https://lucide.dev/icons/layers
 */
export default iconWithClassName(LayersIconBase);
