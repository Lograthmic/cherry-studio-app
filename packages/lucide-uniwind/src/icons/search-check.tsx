import { SearchCheck as SearchCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SearchCheck icon.
 * @see https://lucide.dev/icons/search-check
 */
export default iconWithClassName(SearchCheckIconBase);
