import { FileTerminal as FileTerminalIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileTerminal icon.
 * @see https://lucide.dev/icons/file-terminal
 */
export default iconWithClassName(FileTerminalIconBase);
