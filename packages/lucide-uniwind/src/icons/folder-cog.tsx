import { FolderCog as FolderCogIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderCog icon.
 * @see https://lucide.dev/icons/folder-cog
 */
export default iconWithClassName(FolderCogIconBase);
