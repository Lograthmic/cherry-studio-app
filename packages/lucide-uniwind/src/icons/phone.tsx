import { Phone as PhoneIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Phone icon.
 * @see https://lucide.dev/icons/phone
 */
export default iconWithClassName(PhoneIconBase);
