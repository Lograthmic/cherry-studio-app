import { CameraOff as CameraOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CameraOff icon.
 * @see https://lucide.dev/icons/camera-off
 */
export default iconWithClassName(CameraOffIconBase);
