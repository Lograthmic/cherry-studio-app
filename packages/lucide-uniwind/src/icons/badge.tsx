import { Badge as BadgeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Badge icon.
 * @see https://lucide.dev/icons/badge
 */
export default iconWithClassName(BadgeIconBase);
