import { FileCode as FileCodeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileCode icon.
 * @see https://lucide.dev/icons/file-code
 */
export default iconWithClassName(FileCodeIconBase);
