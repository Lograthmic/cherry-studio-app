import { SearchX as SearchXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SearchX icon.
 * @see https://lucide.dev/icons/search-x
 */
export default iconWithClassName(SearchXIconBase);
