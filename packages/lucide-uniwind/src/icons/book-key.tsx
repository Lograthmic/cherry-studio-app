import { BookKey as BookKeyIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookKey icon.
 * @see https://lucide.dev/icons/book-key
 */
export default iconWithClassName(BookKeyIconBase);
