import { BellCheck as BellCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BellCheck icon.
 * @see https://lucide.dev/icons/bell-check
 */
export default iconWithClassName(BellCheckIconBase);
