import { FingerprintPattern as FingerprintPatternIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FingerprintPattern icon.
 * @see https://lucide.dev/icons/fingerprint-pattern
 */
export default iconWithClassName(FingerprintPatternIconBase);
