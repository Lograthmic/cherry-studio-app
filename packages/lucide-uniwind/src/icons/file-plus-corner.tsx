import { FilePlusCorner as FilePlusCornerIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FilePlusCorner icon.
 * @see https://lucide.dev/icons/file-plus-corner
 */
export default iconWithClassName(FilePlusCornerIconBase);
