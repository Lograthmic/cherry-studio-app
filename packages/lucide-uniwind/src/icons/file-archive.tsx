import { FileArchive as FileArchiveIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileArchive icon.
 * @see https://lucide.dev/icons/file-archive
 */
export default iconWithClassName(FileArchiveIconBase);
