import { Rows as RowsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Rows icon.
 * @see https://lucide.dev/icons/rows
 */
export default iconWithClassName(RowsIconBase);
