import { BeefOff as BeefOffIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BeefOff icon.
 * @see https://lucide.dev/icons/beef-off
 */
export default iconWithClassName(BeefOffIconBase);
