import { NotepadTextDashed as NotepadTextDashedIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * NotepadTextDashed icon.
 * @see https://lucide.dev/icons/notepad-text-dashed
 */
export default iconWithClassName(NotepadTextDashedIconBase);
