import { ArchiveRestore as ArchiveRestoreIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ArchiveRestore icon.
 * @see https://lucide.dev/icons/archive-restore
 */
export default iconWithClassName(ArchiveRestoreIconBase);
