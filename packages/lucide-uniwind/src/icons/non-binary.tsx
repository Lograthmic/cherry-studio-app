import { NonBinary as NonBinaryIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * NonBinary icon.
 * @see https://lucide.dev/icons/non-binary
 */
export default iconWithClassName(NonBinaryIconBase);
