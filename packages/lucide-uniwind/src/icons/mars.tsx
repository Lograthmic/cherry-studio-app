import { Mars as MarsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Mars icon.
 * @see https://lucide.dev/icons/mars
 */
export default iconWithClassName(MarsIconBase);
