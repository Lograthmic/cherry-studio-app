import { Worm as WormIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Worm icon.
 * @see https://lucide.dev/icons/worm
 */
export default iconWithClassName(WormIconBase);
