import { Loader as LoaderIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Loader icon.
 * @see https://lucide.dev/icons/loader
 */
export default iconWithClassName(LoaderIconBase);
