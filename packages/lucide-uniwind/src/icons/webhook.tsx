import { Webhook as WebhookIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Webhook icon.
 * @see https://lucide.dev/icons/webhook
 */
export default iconWithClassName(WebhookIconBase);
