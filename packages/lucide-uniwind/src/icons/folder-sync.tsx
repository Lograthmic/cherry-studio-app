import { FolderSync as FolderSyncIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderSync icon.
 * @see https://lucide.dev/icons/folder-sync
 */
export default iconWithClassName(FolderSyncIconBase);
