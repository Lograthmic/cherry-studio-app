import { Layout as LayoutIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Layout icon.
 * @see https://lucide.dev/icons/layout
 */
export default iconWithClassName(LayoutIconBase);
