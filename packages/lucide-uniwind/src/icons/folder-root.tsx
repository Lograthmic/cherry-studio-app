import { FolderRoot as FolderRootIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderRoot icon.
 * @see https://lucide.dev/icons/folder-root
 */
export default iconWithClassName(FolderRootIconBase);
