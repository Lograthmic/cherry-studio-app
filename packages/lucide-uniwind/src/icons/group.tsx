import { Group as GroupIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Group icon.
 * @see https://lucide.dev/icons/group
 */
export default iconWithClassName(GroupIconBase);
