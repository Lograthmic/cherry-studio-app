import { CloudBackup as CloudBackupIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CloudBackup icon.
 * @see https://lucide.dev/icons/cloud-backup
 */
export default iconWithClassName(CloudBackupIconBase);
