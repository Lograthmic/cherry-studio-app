import { ReplyAll as ReplyAllIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ReplyAll icon.
 * @see https://lucide.dev/icons/reply-all
 */
export default iconWithClassName(ReplyAllIconBase);
