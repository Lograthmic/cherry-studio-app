import { TicketPercent as TicketPercentIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * TicketPercent icon.
 * @see https://lucide.dev/icons/ticket-percent
 */
export default iconWithClassName(TicketPercentIconBase);
