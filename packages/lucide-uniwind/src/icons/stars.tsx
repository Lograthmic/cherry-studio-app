import { Stars as StarsIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Stars icon.
 * @see https://lucide.dev/icons/stars
 */
export default iconWithClassName(StarsIconBase);
