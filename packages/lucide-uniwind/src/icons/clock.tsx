import { Clock as ClockIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Clock icon.
 * @see https://lucide.dev/icons/clock
 */
export default iconWithClassName(ClockIconBase);
