import { Wifi as WifiIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Wifi icon.
 * @see https://lucide.dev/icons/wifi
 */
export default iconWithClassName(WifiIconBase);
