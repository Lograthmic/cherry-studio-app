import { Bomb as BombIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Bomb icon.
 * @see https://lucide.dev/icons/bomb
 */
export default iconWithClassName(BombIconBase);
