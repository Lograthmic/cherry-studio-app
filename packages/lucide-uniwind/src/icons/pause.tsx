import { Pause as PauseIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pause icon.
 * @see https://lucide.dev/icons/pause
 */
export default iconWithClassName(PauseIconBase);
