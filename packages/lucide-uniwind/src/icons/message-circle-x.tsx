import { MessageCircleX as MessageCircleXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * MessageCircleX icon.
 * @see https://lucide.dev/icons/message-circle-x
 */
export default iconWithClassName(MessageCircleXIconBase);
