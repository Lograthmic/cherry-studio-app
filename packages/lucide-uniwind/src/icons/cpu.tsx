import { Cpu as CpuIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Cpu icon.
 * @see https://lucide.dev/icons/cpu
 */
export default iconWithClassName(CpuIconBase);
