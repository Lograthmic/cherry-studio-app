import { FolderOpen as FolderOpenIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FolderOpen icon.
 * @see https://lucide.dev/icons/folder-open
 */
export default iconWithClassName(FolderOpenIconBase);
