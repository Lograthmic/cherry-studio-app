import { BookType as BookTypeIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookType icon.
 * @see https://lucide.dev/icons/book-type
 */
export default iconWithClassName(BookTypeIconBase);
