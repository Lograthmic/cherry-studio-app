import { Wine as WineIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Wine icon.
 * @see https://lucide.dev/icons/wine
 */
export default iconWithClassName(WineIconBase);
