import { FileAudio as FileAudioIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileAudio icon.
 * @see https://lucide.dev/icons/file-audio
 */
export default iconWithClassName(FileAudioIconBase);
