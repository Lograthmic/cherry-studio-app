import { HeartX as HeartXIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * HeartX icon.
 * @see https://lucide.dev/icons/heart-x
 */
export default iconWithClassName(HeartXIconBase);
