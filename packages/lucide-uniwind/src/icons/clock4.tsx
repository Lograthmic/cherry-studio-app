import { Clock4 as Clock4IconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Clock4 icon.
 * @see https://lucide.dev/icons/clock4
 */
export default iconWithClassName(Clock4IconBase);
