import { CalendarCheck as CalendarCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CalendarCheck icon.
 * @see https://lucide.dev/icons/calendar-check
 */
export default iconWithClassName(CalendarCheckIconBase);
