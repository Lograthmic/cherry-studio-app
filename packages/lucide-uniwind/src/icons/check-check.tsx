import { CheckCheck as CheckCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * CheckCheck icon.
 * @see https://lucide.dev/icons/check-check
 */
export default iconWithClassName(CheckCheckIconBase);
