import { Braces as BracesIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Braces icon.
 * @see https://lucide.dev/icons/braces
 */
export default iconWithClassName(BracesIconBase);
