import { SignpostBig as SignpostBigIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SignpostBig icon.
 * @see https://lucide.dev/icons/signpost-big
 */
export default iconWithClassName(SignpostBigIconBase);
