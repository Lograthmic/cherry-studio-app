import { ListRestart as ListRestartIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ListRestart icon.
 * @see https://lucide.dev/icons/list-restart
 */
export default iconWithClassName(ListRestartIconBase);
