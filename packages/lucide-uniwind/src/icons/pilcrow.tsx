import { Pilcrow as PilcrowIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Pilcrow icon.
 * @see https://lucide.dev/icons/pilcrow
 */
export default iconWithClassName(PilcrowIconBase);
