import { Ribbon as RibbonIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Ribbon icon.
 * @see https://lucide.dev/icons/ribbon
 */
export default iconWithClassName(RibbonIconBase);
