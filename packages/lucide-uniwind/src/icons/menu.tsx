import { Menu as MenuIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Menu icon.
 * @see https://lucide.dev/icons/menu
 */
export default iconWithClassName(MenuIconBase);
