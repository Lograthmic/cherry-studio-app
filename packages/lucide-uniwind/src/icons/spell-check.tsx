import { SpellCheck as SpellCheckIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SpellCheck icon.
 * @see https://lucide.dev/icons/spell-check
 */
export default iconWithClassName(SpellCheckIconBase);
