import { FlaskConical as FlaskConicalIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FlaskConical icon.
 * @see https://lucide.dev/icons/flask-conical
 */
export default iconWithClassName(FlaskConicalIconBase);
