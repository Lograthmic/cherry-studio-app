import { ZoomIn as ZoomInIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ZoomIn icon.
 * @see https://lucide.dev/icons/zoom-in
 */
export default iconWithClassName(ZoomInIconBase);
