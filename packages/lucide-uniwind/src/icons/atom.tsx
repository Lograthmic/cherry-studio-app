import { Atom as AtomIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Atom icon.
 * @see https://lucide.dev/icons/atom
 */
export default iconWithClassName(AtomIconBase);
