import { SidebarClose as SidebarCloseIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * SidebarClose icon.
 * @see https://lucide.dev/icons/sidebar-close
 */
export default iconWithClassName(SidebarCloseIconBase);
