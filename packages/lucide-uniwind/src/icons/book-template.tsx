import { BookTemplate as BookTemplateIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * BookTemplate icon.
 * @see https://lucide.dev/icons/book-template
 */
export default iconWithClassName(BookTemplateIconBase);
