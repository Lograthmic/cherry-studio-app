import { FileSignature as FileSignatureIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * FileSignature icon.
 * @see https://lucide.dev/icons/file-signature
 */
export default iconWithClassName(FileSignatureIconBase);
