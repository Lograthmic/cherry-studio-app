import { ShipWheel as ShipWheelIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * ShipWheel icon.
 * @see https://lucide.dev/icons/ship-wheel
 */
export default iconWithClassName(ShipWheelIconBase);
