import { Sidebar as SidebarIconBase } from 'lucide-react-native';
import iconWithClassName from '../iconWithClassName';

/**
 * Sidebar icon.
 * @see https://lucide.dev/icons/sidebar
 */
export default iconWithClassName(SidebarIconBase);
