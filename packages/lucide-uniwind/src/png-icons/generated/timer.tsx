import { createPngIcon } from '../createPngIcon';

/**
 * Timer icon (PNG variant).
 * @see https://lucide.dev/icons/timer
 */
export default createPngIcon(require('../assets/timer.png'), 'TimerIcon');
