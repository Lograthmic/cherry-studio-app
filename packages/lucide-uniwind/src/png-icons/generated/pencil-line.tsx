import { createPngIcon } from '../createPngIcon';

/**
 * PencilLine icon (PNG variant).
 * @see https://lucide.dev/icons/pencil-line
 */
export default createPngIcon(require('../assets/pencil-line.png'), 'PencilLineIcon');
