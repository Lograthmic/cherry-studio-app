import { createPngIcon } from '../createPngIcon';

/**
 * ShieldQuestion icon (PNG variant).
 * @see https://lucide.dev/icons/shield-question
 */
export default createPngIcon(require('../assets/shield-question.png'), 'ShieldQuestionIcon');
