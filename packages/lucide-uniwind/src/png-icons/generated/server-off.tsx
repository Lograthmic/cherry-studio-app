import { createPngIcon } from '../createPngIcon';

/**
 * ServerOff icon (PNG variant).
 * @see https://lucide.dev/icons/server-off
 */
export default createPngIcon(require('../assets/server-off.png'), 'ServerOffIcon');
