import { createPngIcon } from '../createPngIcon';

/**
 * SkipBack icon (PNG variant).
 * @see https://lucide.dev/icons/skip-back
 */
export default createPngIcon(require('../assets/skip-back.png'), 'SkipBackIcon');
