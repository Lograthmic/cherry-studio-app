import { createPngIcon } from '../createPngIcon';

/**
 * PackagePlus icon (PNG variant).
 * @see https://lucide.dev/icons/package-plus
 */
export default createPngIcon(require('../assets/package-plus.png'), 'PackagePlusIcon');
