import { createPngIcon } from '../createPngIcon';

/**
 * Hamburger icon (PNG variant).
 * @see https://lucide.dev/icons/hamburger
 */
export default createPngIcon(require('../assets/hamburger.png'), 'HamburgerIcon');
