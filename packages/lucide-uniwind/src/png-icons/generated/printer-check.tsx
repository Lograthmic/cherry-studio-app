import { createPngIcon } from '../createPngIcon';

/**
 * PrinterCheck icon (PNG variant).
 * @see https://lucide.dev/icons/printer-check
 */
export default createPngIcon(require('../assets/printer-check.png'), 'PrinterCheckIcon');
