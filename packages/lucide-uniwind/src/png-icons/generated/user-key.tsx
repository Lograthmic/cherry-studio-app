import { createPngIcon } from '../createPngIcon';

/**
 * UserKey icon (PNG variant).
 * @see https://lucide.dev/icons/user-key
 */
export default createPngIcon(require('../assets/user-key.png'), 'UserKeyIcon');
