import { createPngIcon } from '../createPngIcon';

/**
 * Sliders icon (PNG variant).
 * @see https://lucide.dev/icons/sliders
 */
export default createPngIcon(require('../assets/sliders.png'), 'SlidersIcon');
