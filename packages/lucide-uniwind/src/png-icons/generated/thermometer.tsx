import { createPngIcon } from '../createPngIcon';

/**
 * Thermometer icon (PNG variant).
 * @see https://lucide.dev/icons/thermometer
 */
export default createPngIcon(require('../assets/thermometer.png'), 'ThermometerIcon');
