import { createPngIcon } from '../createPngIcon';

/**
 * Calendar icon (PNG variant).
 * @see https://lucide.dev/icons/calendar
 */
export default createPngIcon(require('../assets/calendar.png'), 'CalendarIcon');
