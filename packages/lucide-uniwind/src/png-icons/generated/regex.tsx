import { createPngIcon } from '../createPngIcon';

/**
 * Regex icon (PNG variant).
 * @see https://lucide.dev/icons/regex
 */
export default createPngIcon(require('../assets/regex.png'), 'RegexIcon');
