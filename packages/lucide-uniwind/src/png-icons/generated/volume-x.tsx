import { createPngIcon } from '../createPngIcon';

/**
 * VolumeX icon (PNG variant).
 * @see https://lucide.dev/icons/volume-x
 */
export default createPngIcon(require('../assets/volume-x.png'), 'VolumeXIcon');
