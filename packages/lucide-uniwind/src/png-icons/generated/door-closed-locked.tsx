import { createPngIcon } from '../createPngIcon';

/**
 * DoorClosedLocked icon (PNG variant).
 * @see https://lucide.dev/icons/door-closed-locked
 */
export default createPngIcon(require('../assets/door-closed-locked.png'), 'DoorClosedLockedIcon');
