import { createPngIcon } from '../createPngIcon';

/**
 * Clock icon (PNG variant).
 * @see https://lucide.dev/icons/clock
 */
export default createPngIcon(require('../assets/clock.png'), 'ClockIcon');
