import { createPngIcon } from '../createPngIcon';

/**
 * FolderCode icon (PNG variant).
 * @see https://lucide.dev/icons/folder-code
 */
export default createPngIcon(require('../assets/folder-code.png'), 'FolderCodeIcon');
