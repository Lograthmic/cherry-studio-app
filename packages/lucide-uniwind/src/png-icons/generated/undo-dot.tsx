import { createPngIcon } from '../createPngIcon';

/**
 * UndoDot icon (PNG variant).
 * @see https://lucide.dev/icons/undo-dot
 */
export default createPngIcon(require('../assets/undo-dot.png'), 'UndoDotIcon');
