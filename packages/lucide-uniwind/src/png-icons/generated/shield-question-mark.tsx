import { createPngIcon } from '../createPngIcon';

/**
 * ShieldQuestionMark icon (PNG variant).
 * @see https://lucide.dev/icons/shield-question-mark
 */
export default createPngIcon(require('../assets/shield-question-mark.png'), 'ShieldQuestionMarkIcon');
