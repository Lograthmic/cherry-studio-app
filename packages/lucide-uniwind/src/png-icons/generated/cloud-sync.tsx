import { createPngIcon } from '../createPngIcon';

/**
 * CloudSync icon (PNG variant).
 * @see https://lucide.dev/icons/cloud-sync
 */
export default createPngIcon(require('../assets/cloud-sync.png'), 'CloudSyncIcon');
