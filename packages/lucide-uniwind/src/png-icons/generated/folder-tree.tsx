import { createPngIcon } from '../createPngIcon';

/**
 * FolderTree icon (PNG variant).
 * @see https://lucide.dev/icons/folder-tree
 */
export default createPngIcon(require('../assets/folder-tree.png'), 'FolderTreeIcon');
