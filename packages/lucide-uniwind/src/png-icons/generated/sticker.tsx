import { createPngIcon } from '../createPngIcon';

/**
 * Sticker icon (PNG variant).
 * @see https://lucide.dev/icons/sticker
 */
export default createPngIcon(require('../assets/sticker.png'), 'StickerIcon');
