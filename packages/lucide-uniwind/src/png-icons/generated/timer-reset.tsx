import { createPngIcon } from '../createPngIcon';

/**
 * TimerReset icon (PNG variant).
 * @see https://lucide.dev/icons/timer-reset
 */
export default createPngIcon(require('../assets/timer-reset.png'), 'TimerResetIcon');
