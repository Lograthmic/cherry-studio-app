import { createPngIcon } from '../createPngIcon';

/**
 * Check icon (PNG variant).
 * @see https://lucide.dev/icons/check
 */
export default createPngIcon(require('../assets/check.png'), 'CheckIcon');
