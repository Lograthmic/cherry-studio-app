import { createPngIcon } from '../createPngIcon';

/**
 * ShieldCheck icon (PNG variant).
 * @see https://lucide.dev/icons/shield-check
 */
export default createPngIcon(require('../assets/shield-check.png'), 'ShieldCheckIcon');
