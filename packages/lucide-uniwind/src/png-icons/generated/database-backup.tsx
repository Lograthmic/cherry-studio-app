import { createPngIcon } from '../createPngIcon';

/**
 * DatabaseBackup icon (PNG variant).
 * @see https://lucide.dev/icons/database-backup
 */
export default createPngIcon(require('../assets/database-backup.png'), 'DatabaseBackupIcon');
