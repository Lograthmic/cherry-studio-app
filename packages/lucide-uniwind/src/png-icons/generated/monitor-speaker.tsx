import { createPngIcon } from '../createPngIcon';

/**
 * MonitorSpeaker icon (PNG variant).
 * @see https://lucide.dev/icons/monitor-speaker
 */
export default createPngIcon(require('../assets/monitor-speaker.png'), 'MonitorSpeakerIcon');
