import { createPngIcon } from '../createPngIcon';

/**
 * Server icon (PNG variant).
 * @see https://lucide.dev/icons/server
 */
export default createPngIcon(require('../assets/server.png'), 'ServerIcon');
