import { createPngIcon } from '../createPngIcon';

/**
 * TrainFrontTunnel icon (PNG variant).
 * @see https://lucide.dev/icons/train-front-tunnel
 */
export default createPngIcon(require('../assets/train-front-tunnel.png'), 'TrainFrontTunnelIcon');
