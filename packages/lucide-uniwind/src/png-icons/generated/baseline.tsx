import { createPngIcon } from '../createPngIcon';

/**
 * Baseline icon (PNG variant).
 * @see https://lucide.dev/icons/baseline
 */
export default createPngIcon(require('../assets/baseline.png'), 'BaselineIcon');
