import { createPngIcon } from '../createPngIcon';

/**
 * Maximize icon (PNG variant).
 * @see https://lucide.dev/icons/maximize
 */
export default createPngIcon(require('../assets/maximize.png'), 'MaximizeIcon');
