import { createPngIcon } from '../createPngIcon';

/**
 * Snowflake icon (PNG variant).
 * @see https://lucide.dev/icons/snowflake
 */
export default createPngIcon(require('../assets/snowflake.png'), 'SnowflakeIcon');
