import { createPngIcon } from '../createPngIcon';

/**
 * Reply icon (PNG variant).
 * @see https://lucide.dev/icons/reply
 */
export default createPngIcon(require('../assets/reply.png'), 'ReplyIcon');
