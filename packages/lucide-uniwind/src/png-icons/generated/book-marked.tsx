import { createPngIcon } from '../createPngIcon';

/**
 * BookMarked icon (PNG variant).
 * @see https://lucide.dev/icons/book-marked
 */
export default createPngIcon(require('../assets/book-marked.png'), 'BookMarkedIcon');
