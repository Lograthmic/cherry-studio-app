import { createPngIcon } from '../createPngIcon';

/**
 * Unlock icon (PNG variant).
 * @see https://lucide.dev/icons/unlock
 */
export default createPngIcon(require('../assets/unlock.png'), 'UnlockIcon');
