import { createPngIcon } from '../createPngIcon';

/**
 * Fence icon (PNG variant).
 * @see https://lucide.dev/icons/fence
 */
export default createPngIcon(require('../assets/fence.png'), 'FenceIcon');
