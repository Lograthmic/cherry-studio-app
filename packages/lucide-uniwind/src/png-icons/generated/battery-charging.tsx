import { createPngIcon } from '../createPngIcon';

/**
 * BatteryCharging icon (PNG variant).
 * @see https://lucide.dev/icons/battery-charging
 */
export default createPngIcon(require('../assets/battery-charging.png'), 'BatteryChargingIcon');
