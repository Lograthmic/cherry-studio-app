import { createPngIcon } from '../createPngIcon';

/**
 * CreditCard icon (PNG variant).
 * @see https://lucide.dev/icons/credit-card
 */
export default createPngIcon(require('../assets/credit-card.png'), 'CreditCardIcon');
