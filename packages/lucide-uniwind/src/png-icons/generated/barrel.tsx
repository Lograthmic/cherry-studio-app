import { createPngIcon } from '../createPngIcon';

/**
 * Barrel icon (PNG variant).
 * @see https://lucide.dev/icons/barrel
 */
export default createPngIcon(require('../assets/barrel.png'), 'BarrelIcon');
