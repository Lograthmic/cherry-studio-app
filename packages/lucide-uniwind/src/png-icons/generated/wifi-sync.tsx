import { createPngIcon } from '../createPngIcon';

/**
 * WifiSync icon (PNG variant).
 * @see https://lucide.dev/icons/wifi-sync
 */
export default createPngIcon(require('../assets/wifi-sync.png'), 'WifiSyncIcon');
