import { createPngIcon } from '../createPngIcon';

/**
 * AtSign icon (PNG variant).
 * @see https://lucide.dev/icons/at-sign
 */
export default createPngIcon(require('../assets/at-sign.png'), 'AtSignIcon');
