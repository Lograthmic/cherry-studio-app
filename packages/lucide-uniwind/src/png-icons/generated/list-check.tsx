import { createPngIcon } from '../createPngIcon';

/**
 * ListCheck icon (PNG variant).
 * @see https://lucide.dev/icons/list-check
 */
export default createPngIcon(require('../assets/list-check.png'), 'ListCheckIcon');
