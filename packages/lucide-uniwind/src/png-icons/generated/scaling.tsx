import { createPngIcon } from '../createPngIcon';

/**
 * Scaling icon (PNG variant).
 * @see https://lucide.dev/icons/scaling
 */
export default createPngIcon(require('../assets/scaling.png'), 'ScalingIcon');
