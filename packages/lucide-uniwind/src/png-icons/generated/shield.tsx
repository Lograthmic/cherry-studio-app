import { createPngIcon } from '../createPngIcon';

/**
 * Shield icon (PNG variant).
 * @see https://lucide.dev/icons/shield
 */
export default createPngIcon(require('../assets/shield.png'), 'ShieldIcon');
