import { createPngIcon } from '../createPngIcon';

/**
 * FilePlusCorner icon (PNG variant).
 * @see https://lucide.dev/icons/file-plus-corner
 */
export default createPngIcon(require('../assets/file-plus-corner.png'), 'FilePlusCornerIcon');
