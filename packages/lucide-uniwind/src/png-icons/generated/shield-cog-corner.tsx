import { createPngIcon } from '../createPngIcon';

/**
 * ShieldCogCorner icon (PNG variant).
 * @see https://lucide.dev/icons/shield-cog-corner
 */
export default createPngIcon(require('../assets/shield-cog-corner.png'), 'ShieldCogCornerIcon');
