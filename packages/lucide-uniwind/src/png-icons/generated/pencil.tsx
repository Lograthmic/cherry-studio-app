import { createPngIcon } from '../createPngIcon';

/**
 * Pencil icon (PNG variant).
 * @see https://lucide.dev/icons/pencil
 */
export default createPngIcon(require('../assets/pencil.png'), 'PencilIcon');
