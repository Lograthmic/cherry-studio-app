import { createPngIcon } from '../createPngIcon';

/**
 * Merge icon (PNG variant).
 * @see https://lucide.dev/icons/merge
 */
export default createPngIcon(require('../assets/merge.png'), 'MergeIcon');
