import { createPngIcon } from '../createPngIcon';

/**
 * CloudBackup icon (PNG variant).
 * @see https://lucide.dev/icons/cloud-backup
 */
export default createPngIcon(require('../assets/cloud-backup.png'), 'CloudBackupIcon');
