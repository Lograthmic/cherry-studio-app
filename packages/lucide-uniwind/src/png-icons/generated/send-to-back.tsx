import { createPngIcon } from '../createPngIcon';

/**
 * SendToBack icon (PNG variant).
 * @see https://lucide.dev/icons/send-to-back
 */
export default createPngIcon(require('../assets/send-to-back.png'), 'SendToBackIcon');
