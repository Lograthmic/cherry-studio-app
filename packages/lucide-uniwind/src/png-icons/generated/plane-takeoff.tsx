import { createPngIcon } from '../createPngIcon';

/**
 * PlaneTakeoff icon (PNG variant).
 * @see https://lucide.dev/icons/plane-takeoff
 */
export default createPngIcon(require('../assets/plane-takeoff.png'), 'PlaneTakeoffIcon');
