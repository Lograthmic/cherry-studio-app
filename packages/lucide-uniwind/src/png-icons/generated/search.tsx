import { createPngIcon } from '../createPngIcon';

/**
 * Search icon (PNG variant).
 * @see https://lucide.dev/icons/search
 */
export default createPngIcon(require('../assets/search.png'), 'SearchIcon');
