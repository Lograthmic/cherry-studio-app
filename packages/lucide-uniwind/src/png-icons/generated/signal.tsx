import { createPngIcon } from '../createPngIcon';

/**
 * Signal icon (PNG variant).
 * @see https://lucide.dev/icons/signal
 */
export default createPngIcon(require('../assets/signal.png'), 'SignalIcon');
