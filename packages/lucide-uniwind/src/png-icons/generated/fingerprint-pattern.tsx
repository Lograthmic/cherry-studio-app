import { createPngIcon } from '../createPngIcon';

/**
 * FingerprintPattern icon (PNG variant).
 * @see https://lucide.dev/icons/fingerprint-pattern
 */
export default createPngIcon(require('../assets/fingerprint-pattern.png'), 'FingerprintPatternIcon');
