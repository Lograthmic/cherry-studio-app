import { createPngIcon } from '../createPngIcon';

/**
 * NonBinary icon (PNG variant).
 * @see https://lucide.dev/icons/non-binary
 */
export default createPngIcon(require('../assets/non-binary.png'), 'NonBinaryIcon');
