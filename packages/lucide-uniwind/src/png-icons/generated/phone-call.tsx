import { createPngIcon } from '../createPngIcon';

/**
 * PhoneCall icon (PNG variant).
 * @see https://lucide.dev/icons/phone-call
 */
export default createPngIcon(require('../assets/phone-call.png'), 'PhoneCallIcon');
