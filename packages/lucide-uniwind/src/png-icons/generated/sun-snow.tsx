import { createPngIcon } from '../createPngIcon';

/**
 * SunSnow icon (PNG variant).
 * @see https://lucide.dev/icons/sun-snow
 */
export default createPngIcon(require('../assets/sun-snow.png'), 'SunSnowIcon');
