import { createPngIcon } from '../createPngIcon';

/**
 * ClipboardList icon (PNG variant).
 * @see https://lucide.dev/icons/clipboard-list
 */
export default createPngIcon(require('../assets/clipboard-list.png'), 'ClipboardListIcon');
