import { createPngIcon } from '../createPngIcon';

/**
 * ReplaceAll icon (PNG variant).
 * @see https://lucide.dev/icons/replace-all
 */
export default createPngIcon(require('../assets/replace-all.png'), 'ReplaceAllIcon');
