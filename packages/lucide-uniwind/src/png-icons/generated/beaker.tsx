import { createPngIcon } from '../createPngIcon';

/**
 * Beaker icon (PNG variant).
 * @see https://lucide.dev/icons/beaker
 */
export default createPngIcon(require('../assets/beaker.png'), 'BeakerIcon');
