import { createPngIcon } from '../createPngIcon';

/**
 * CardSim icon (PNG variant).
 * @see https://lucide.dev/icons/card-sim
 */
export default createPngIcon(require('../assets/card-sim.png'), 'CardSimIcon');
