import { createPngIcon } from '../createPngIcon';

/**
 * ClockPlus icon (PNG variant).
 * @see https://lucide.dev/icons/clock-plus
 */
export default createPngIcon(require('../assets/clock-plus.png'), 'ClockPlusIcon');
