import { createPngIcon } from '../createPngIcon';

/**
 * Printer icon (PNG variant).
 * @see https://lucide.dev/icons/printer
 */
export default createPngIcon(require('../assets/printer.png'), 'PrinterIcon');
