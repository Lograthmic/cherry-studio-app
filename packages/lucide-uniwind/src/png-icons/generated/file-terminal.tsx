import { createPngIcon } from '../createPngIcon';

/**
 * FileTerminal icon (PNG variant).
 * @see https://lucide.dev/icons/file-terminal
 */
export default createPngIcon(require('../assets/file-terminal.png'), 'FileTerminalIcon');
