import { createPngIcon } from '../createPngIcon';

/**
 * Router icon (PNG variant).
 * @see https://lucide.dev/icons/router
 */
export default createPngIcon(require('../assets/router.png'), 'RouterIcon');
