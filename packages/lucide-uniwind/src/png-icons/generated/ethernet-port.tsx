import { createPngIcon } from '../createPngIcon';

/**
 * EthernetPort icon (PNG variant).
 * @see https://lucide.dev/icons/ethernet-port
 */
export default createPngIcon(require('../assets/ethernet-port.png'), 'EthernetPortIcon');
