import { createPngIcon } from '../createPngIcon';

/**
 * NotepadTextDashed icon (PNG variant).
 * @see https://lucide.dev/icons/notepad-text-dashed
 */
export default createPngIcon(require('../assets/notepad-text-dashed.png'), 'NotepadTextDashedIcon');
