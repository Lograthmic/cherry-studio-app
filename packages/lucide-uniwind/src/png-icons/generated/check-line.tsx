import { createPngIcon } from '../createPngIcon';

/**
 * CheckLine icon (PNG variant).
 * @see https://lucide.dev/icons/check-line
 */
export default createPngIcon(require('../assets/check-line.png'), 'CheckLineIcon');
