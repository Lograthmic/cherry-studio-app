import { createPngIcon } from '../createPngIcon';

/**
 * PackageX icon (PNG variant).
 * @see https://lucide.dev/icons/package-x
 */
export default createPngIcon(require('../assets/package-x.png'), 'PackageXIcon');
