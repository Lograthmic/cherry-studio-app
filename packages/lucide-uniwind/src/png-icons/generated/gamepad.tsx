import { createPngIcon } from '../createPngIcon';

/**
 * Gamepad icon (PNG variant).
 * @see https://lucide.dev/icons/gamepad
 */
export default createPngIcon(require('../assets/gamepad.png'), 'GamepadIcon');
