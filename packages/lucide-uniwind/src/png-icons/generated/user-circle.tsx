import { createPngIcon } from '../createPngIcon';

/**
 * UserCircle icon (PNG variant).
 * @see https://lucide.dev/icons/user-circle
 */
export default createPngIcon(require('../assets/user-circle.png'), 'UserCircleIcon');
