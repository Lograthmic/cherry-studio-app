import { createPngIcon } from '../createPngIcon';

/**
 * UserCheck icon (PNG variant).
 * @see https://lucide.dev/icons/user-check
 */
export default createPngIcon(require('../assets/user-check.png'), 'UserCheckIcon');
