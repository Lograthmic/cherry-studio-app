import { createPngIcon } from '../createPngIcon';

/**
 * Aperture icon (PNG variant).
 * @see https://lucide.dev/icons/aperture
 */
export default createPngIcon(require('../assets/aperture.png'), 'ApertureIcon');
