import { createPngIcon } from '../createPngIcon';

/**
 * PiggyBank icon (PNG variant).
 * @see https://lucide.dev/icons/piggy-bank
 */
export default createPngIcon(require('../assets/piggy-bank.png'), 'PiggyBankIcon');
