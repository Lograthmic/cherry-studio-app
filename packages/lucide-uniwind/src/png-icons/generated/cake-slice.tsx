import { createPngIcon } from '../createPngIcon';

/**
 * CakeSlice icon (PNG variant).
 * @see https://lucide.dev/icons/cake-slice
 */
export default createPngIcon(require('../assets/cake-slice.png'), 'CakeSliceIcon');
