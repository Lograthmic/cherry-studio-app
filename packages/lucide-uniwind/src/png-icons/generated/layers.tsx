import { createPngIcon } from '../createPngIcon';

/**
 * Layers icon (PNG variant).
 * @see https://lucide.dev/icons/layers
 */
export default createPngIcon(require('../assets/layers.png'), 'LayersIcon');
