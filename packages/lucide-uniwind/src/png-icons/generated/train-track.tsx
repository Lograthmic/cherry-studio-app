import { createPngIcon } from '../createPngIcon';

/**
 * TrainTrack icon (PNG variant).
 * @see https://lucide.dev/icons/train-track
 */
export default createPngIcon(require('../assets/train-track.png'), 'TrainTrackIcon');
