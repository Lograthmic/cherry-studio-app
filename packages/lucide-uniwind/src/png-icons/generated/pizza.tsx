import { createPngIcon } from '../createPngIcon';

/**
 * Pizza icon (PNG variant).
 * @see https://lucide.dev/icons/pizza
 */
export default createPngIcon(require('../assets/pizza.png'), 'PizzaIcon');
