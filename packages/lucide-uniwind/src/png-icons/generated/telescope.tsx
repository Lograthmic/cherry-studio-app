import { createPngIcon } from '../createPngIcon';

/**
 * Telescope icon (PNG variant).
 * @see https://lucide.dev/icons/telescope
 */
export default createPngIcon(require('../assets/telescope.png'), 'TelescopeIcon');
