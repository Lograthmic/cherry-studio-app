import { createPngIcon } from '../createPngIcon';

/**
 * FolderOpen icon (PNG variant).
 * @see https://lucide.dev/icons/folder-open
 */
export default createPngIcon(require('../assets/folder-open.png'), 'FolderOpenIcon');
