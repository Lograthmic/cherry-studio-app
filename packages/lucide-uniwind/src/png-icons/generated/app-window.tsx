import { createPngIcon } from '../createPngIcon';

/**
 * AppWindow icon (PNG variant).
 * @see https://lucide.dev/icons/app-window
 */
export default createPngIcon(require('../assets/app-window.png'), 'AppWindowIcon');
