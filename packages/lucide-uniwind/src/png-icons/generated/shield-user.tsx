import { createPngIcon } from '../createPngIcon';

/**
 * ShieldUser icon (PNG variant).
 * @see https://lucide.dev/icons/shield-user
 */
export default createPngIcon(require('../assets/shield-user.png'), 'ShieldUserIcon');
