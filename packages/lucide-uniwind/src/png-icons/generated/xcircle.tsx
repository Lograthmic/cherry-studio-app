import { createPngIcon } from '../createPngIcon';

/**
 * XCircle icon (PNG variant).
 * @see https://lucide.dev/icons/xcircle
 */
export default createPngIcon(require('../assets/xcircle.png'), 'XCircleIcon');
