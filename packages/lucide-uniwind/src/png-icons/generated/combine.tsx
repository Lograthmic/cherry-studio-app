import { createPngIcon } from '../createPngIcon';

/**
 * Combine icon (PNG variant).
 * @see https://lucide.dev/icons/combine
 */
export default createPngIcon(require('../assets/combine.png'), 'CombineIcon');
