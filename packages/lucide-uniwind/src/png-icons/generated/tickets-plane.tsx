import { createPngIcon } from '../createPngIcon';

/**
 * TicketsPlane icon (PNG variant).
 * @see https://lucide.dev/icons/tickets-plane
 */
export default createPngIcon(require('../assets/tickets-plane.png'), 'TicketsPlaneIcon');
