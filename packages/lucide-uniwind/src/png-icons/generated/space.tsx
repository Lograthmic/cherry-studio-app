import { createPngIcon } from '../createPngIcon';

/**
 * Space icon (PNG variant).
 * @see https://lucide.dev/icons/space
 */
export default createPngIcon(require('../assets/space.png'), 'SpaceIcon');
