import { createPngIcon } from '../createPngIcon';

/**
 * Tickets icon (PNG variant).
 * @see https://lucide.dev/icons/tickets
 */
export default createPngIcon(require('../assets/tickets.png'), 'TicketsIcon');
