import { createPngIcon } from '../createPngIcon';

/**
 * ChevronFirst icon (PNG variant).
 * @see https://lucide.dev/icons/chevron-first
 */
export default createPngIcon(require('../assets/chevron-first.png'), 'ChevronFirstIcon');
