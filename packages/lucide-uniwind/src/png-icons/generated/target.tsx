import { createPngIcon } from '../createPngIcon';

/**
 * Target icon (PNG variant).
 * @see https://lucide.dev/icons/target
 */
export default createPngIcon(require('../assets/target.png'), 'TargetIcon');
