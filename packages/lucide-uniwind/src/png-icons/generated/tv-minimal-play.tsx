import { createPngIcon } from '../createPngIcon';

/**
 * TvMinimalPlay icon (PNG variant).
 * @see https://lucide.dev/icons/tv-minimal-play
 */
export default createPngIcon(require('../assets/tv-minimal-play.png'), 'TvMinimalPlayIcon');
