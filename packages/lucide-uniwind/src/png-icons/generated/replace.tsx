import { createPngIcon } from '../createPngIcon';

/**
 * Replace icon (PNG variant).
 * @see https://lucide.dev/icons/replace
 */
export default createPngIcon(require('../assets/replace.png'), 'ReplaceIcon');
