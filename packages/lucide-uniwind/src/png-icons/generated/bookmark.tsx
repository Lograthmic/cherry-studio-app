import { createPngIcon } from '../createPngIcon';

/**
 * Bookmark icon (PNG variant).
 * @see https://lucide.dev/icons/bookmark
 */
export default createPngIcon(require('../assets/bookmark.png'), 'BookmarkIcon');
