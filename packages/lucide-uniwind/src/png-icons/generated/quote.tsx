import { createPngIcon } from '../createPngIcon';

/**
 * Quote icon (PNG variant).
 * @see https://lucide.dev/icons/quote
 */
export default createPngIcon(require('../assets/quote.png'), 'QuoteIcon');
