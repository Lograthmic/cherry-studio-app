import { createPngIcon } from '../createPngIcon';

/**
 * Coins icon (PNG variant).
 * @see https://lucide.dev/icons/coins
 */
export default createPngIcon(require('../assets/coins.png'), 'CoinsIcon');
