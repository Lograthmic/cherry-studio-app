import { createPngIcon } from '../createPngIcon';

/**
 * ServerCrash icon (PNG variant).
 * @see https://lucide.dev/icons/server-crash
 */
export default createPngIcon(require('../assets/server-crash.png'), 'ServerCrashIcon');
