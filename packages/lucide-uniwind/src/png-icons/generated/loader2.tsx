import { createPngIcon } from '../createPngIcon';

/**
 * Loader2 icon (PNG variant).
 * @see https://lucide.dev/icons/loader2
 */
export default createPngIcon(require('../assets/loader2.png'), 'Loader2Icon');
