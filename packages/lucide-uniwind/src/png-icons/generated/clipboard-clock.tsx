import { createPngIcon } from '../createPngIcon';

/**
 * ClipboardClock icon (PNG variant).
 * @see https://lucide.dev/icons/clipboard-clock
 */
export default createPngIcon(require('../assets/clipboard-clock.png'), 'ClipboardClockIcon');
