import { createPngIcon } from '../createPngIcon';

/**
 * Contrast icon (PNG variant).
 * @see https://lucide.dev/icons/contrast
 */
export default createPngIcon(require('../assets/contrast.png'), 'ContrastIcon');
