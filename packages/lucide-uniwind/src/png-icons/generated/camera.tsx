import { createPngIcon } from '../createPngIcon';

/**
 * Camera icon (PNG variant).
 * @see https://lucide.dev/icons/camera
 */
export default createPngIcon(require('../assets/camera.png'), 'CameraIcon');
