import { createPngIcon } from '../createPngIcon';

/**
 * MapPinned icon (PNG variant).
 * @see https://lucide.dev/icons/map-pinned
 */
export default createPngIcon(require('../assets/map-pinned.png'), 'MapPinnedIcon');
