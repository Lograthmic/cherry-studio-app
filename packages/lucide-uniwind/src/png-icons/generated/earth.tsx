import { createPngIcon } from '../createPngIcon';

/**
 * Earth icon (PNG variant).
 * @see https://lucide.dev/icons/earth
 */
export default createPngIcon(require('../assets/earth.png'), 'EarthIcon');
