import { createPngIcon } from '../createPngIcon';

/**
 * FolderBookmark icon (PNG variant).
 * @see https://lucide.dev/icons/folder-bookmark
 */
export default createPngIcon(require('../assets/folder-bookmark.png'), 'FolderBookmarkIcon');
