import { createPngIcon } from '../createPngIcon';

/**
 * PencilRuler icon (PNG variant).
 * @see https://lucide.dev/icons/pencil-ruler
 */
export default createPngIcon(require('../assets/pencil-ruler.png'), 'PencilRulerIcon');
