import { createPngIcon } from '../createPngIcon';

/**
 * Webhook icon (PNG variant).
 * @see https://lucide.dev/icons/webhook
 */
export default createPngIcon(require('../assets/webhook.png'), 'WebhookIcon');
