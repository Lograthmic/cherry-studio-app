import { createPngIcon } from '../createPngIcon';

/**
 * Pause icon (PNG variant).
 * @see https://lucide.dev/icons/pause
 */
export default createPngIcon(require('../assets/pause.png'), 'PauseIcon');
