import { createPngIcon } from '../createPngIcon';

/**
 * SortDesc icon (PNG variant).
 * @see https://lucide.dev/icons/sort-desc
 */
export default createPngIcon(require('../assets/sort-desc.png'), 'SortDescIcon');
