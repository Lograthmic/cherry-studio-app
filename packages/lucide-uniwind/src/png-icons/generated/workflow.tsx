import { createPngIcon } from '../createPngIcon';

/**
 * Workflow icon (PNG variant).
 * @see https://lucide.dev/icons/workflow
 */
export default createPngIcon(require('../assets/workflow.png'), 'WorkflowIcon');
