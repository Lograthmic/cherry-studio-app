import { createPngIcon } from '../createPngIcon';

/**
 * Blinds icon (PNG variant).
 * @see https://lucide.dev/icons/blinds
 */
export default createPngIcon(require('../assets/blinds.png'), 'BlindsIcon');
