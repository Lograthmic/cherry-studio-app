import { createPngIcon } from '../createPngIcon';

/**
 * SplinePointer icon (PNG variant).
 * @see https://lucide.dev/icons/spline-pointer
 */
export default createPngIcon(require('../assets/spline-pointer.png'), 'SplinePointerIcon');
