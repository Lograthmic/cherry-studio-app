import { createPngIcon } from '../createPngIcon';

/**
 * UserRoundX icon (PNG variant).
 * @see https://lucide.dev/icons/user-round-x
 */
export default createPngIcon(require('../assets/user-round-x.png'), 'UserRoundXIcon');
