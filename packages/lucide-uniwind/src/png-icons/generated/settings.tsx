import { createPngIcon } from '../createPngIcon';

/**
 * Settings icon (PNG variant).
 * @see https://lucide.dev/icons/settings
 */
export default createPngIcon(require('../assets/settings.png'), 'SettingsIcon');
