import { createPngIcon } from '../createPngIcon';

/**
 * Database icon (PNG variant).
 * @see https://lucide.dev/icons/database
 */
export default createPngIcon(require('../assets/database.png'), 'DatabaseIcon');
