import { createPngIcon } from '../createPngIcon';

/**
 * Folders icon (PNG variant).
 * @see https://lucide.dev/icons/folders
 */
export default createPngIcon(require('../assets/folders.png'), 'FoldersIcon');
