import { createPngIcon } from '../createPngIcon';

/**
 * Ruler icon (PNG variant).
 * @see https://lucide.dev/icons/ruler
 */
export default createPngIcon(require('../assets/ruler.png'), 'RulerIcon');
