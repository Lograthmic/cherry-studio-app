import { createPngIcon } from '../createPngIcon';

/**
 * FolderArchive icon (PNG variant).
 * @see https://lucide.dev/icons/folder-archive
 */
export default createPngIcon(require('../assets/folder-archive.png'), 'FolderArchiveIcon');
