import { createPngIcon } from '../createPngIcon';

/**
 * PackageSearch icon (PNG variant).
 * @see https://lucide.dev/icons/package-search
 */
export default createPngIcon(require('../assets/package-search.png'), 'PackageSearchIcon');
