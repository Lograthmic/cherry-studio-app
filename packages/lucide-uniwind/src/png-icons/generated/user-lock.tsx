import { createPngIcon } from '../createPngIcon';

/**
 * UserLock icon (PNG variant).
 * @see https://lucide.dev/icons/user-lock
 */
export default createPngIcon(require('../assets/user-lock.png'), 'UserLockIcon');
