import { createPngIcon } from '../createPngIcon';

/**
 * FunnelPlus icon (PNG variant).
 * @see https://lucide.dev/icons/funnel-plus
 */
export default createPngIcon(require('../assets/funnel-plus.png'), 'FunnelPlusIcon');
