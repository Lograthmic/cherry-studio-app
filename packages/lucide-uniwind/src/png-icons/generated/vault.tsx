import { createPngIcon } from '../createPngIcon';

/**
 * Vault icon (PNG variant).
 * @see https://lucide.dev/icons/vault
 */
export default createPngIcon(require('../assets/vault.png'), 'VaultIcon');
