import { createPngIcon } from '../createPngIcon';

/**
 * NotepadText icon (PNG variant).
 * @see https://lucide.dev/icons/notepad-text
 */
export default createPngIcon(require('../assets/notepad-text.png'), 'NotepadTextIcon');
