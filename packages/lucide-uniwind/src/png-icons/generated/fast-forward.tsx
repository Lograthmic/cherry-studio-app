import { createPngIcon } from '../createPngIcon';

/**
 * FastForward icon (PNG variant).
 * @see https://lucide.dev/icons/fast-forward
 */
export default createPngIcon(require('../assets/fast-forward.png'), 'FastForwardIcon');
