import { createPngIcon } from '../createPngIcon';

/**
 * ParkingMeter icon (PNG variant).
 * @see https://lucide.dev/icons/parking-meter
 */
export default createPngIcon(require('../assets/parking-meter.png'), 'ParkingMeterIcon');
