import { createPngIcon } from '../createPngIcon';

/**
 * Clock10 icon (PNG variant).
 * @see https://lucide.dev/icons/clock10
 */
export default createPngIcon(require('../assets/clock10.png'), 'Clock10Icon');
