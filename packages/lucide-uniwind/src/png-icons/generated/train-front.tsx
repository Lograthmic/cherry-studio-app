import { createPngIcon } from '../createPngIcon';

/**
 * TrainFront icon (PNG variant).
 * @see https://lucide.dev/icons/train-front
 */
export default createPngIcon(require('../assets/train-front.png'), 'TrainFrontIcon');
