import { createPngIcon } from '../createPngIcon';

/**
 * ShipWheel icon (PNG variant).
 * @see https://lucide.dev/icons/ship-wheel
 */
export default createPngIcon(require('../assets/ship-wheel.png'), 'ShipWheelIcon');
