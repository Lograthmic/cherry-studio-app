import { createPngIcon } from '../createPngIcon';

/**
 * Speaker icon (PNG variant).
 * @see https://lucide.dev/icons/speaker
 */
export default createPngIcon(require('../assets/speaker.png'), 'SpeakerIcon');
