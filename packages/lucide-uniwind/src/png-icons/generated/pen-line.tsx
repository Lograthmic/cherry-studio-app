import { createPngIcon } from '../createPngIcon';

/**
 * PenLine icon (PNG variant).
 * @see https://lucide.dev/icons/pen-line
 */
export default createPngIcon(require('../assets/pen-line.png'), 'PenLineIcon');
