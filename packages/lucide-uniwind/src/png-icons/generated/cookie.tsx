import { createPngIcon } from '../createPngIcon';

/**
 * Cookie icon (PNG variant).
 * @see https://lucide.dev/icons/cookie
 */
export default createPngIcon(require('../assets/cookie.png'), 'CookieIcon');
