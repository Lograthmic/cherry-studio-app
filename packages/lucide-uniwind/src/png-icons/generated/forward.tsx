import { createPngIcon } from '../createPngIcon';

/**
 * Forward icon (PNG variant).
 * @see https://lucide.dev/icons/forward
 */
export default createPngIcon(require('../assets/forward.png'), 'ForwardIcon');
