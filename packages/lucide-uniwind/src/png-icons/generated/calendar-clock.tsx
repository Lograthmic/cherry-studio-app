import { createPngIcon } from '../createPngIcon';

/**
 * CalendarClock icon (PNG variant).
 * @see https://lucide.dev/icons/calendar-clock
 */
export default createPngIcon(require('../assets/calendar-clock.png'), 'CalendarClockIcon');
