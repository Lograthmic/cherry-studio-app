import { createPngIcon } from '../createPngIcon';

/**
 * SignpostBig icon (PNG variant).
 * @see https://lucide.dev/icons/signpost-big
 */
export default createPngIcon(require('../assets/signpost-big.png'), 'SignpostBigIcon');
