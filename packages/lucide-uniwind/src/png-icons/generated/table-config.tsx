import { createPngIcon } from '../createPngIcon';

/**
 * TableConfig icon (PNG variant).
 * @see https://lucide.dev/icons/table-config
 */
export default createPngIcon(require('../assets/table-config.png'), 'TableConfigIcon');
