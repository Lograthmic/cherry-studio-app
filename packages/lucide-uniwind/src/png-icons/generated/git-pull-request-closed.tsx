import { createPngIcon } from '../createPngIcon';

/**
 * GitPullRequestClosed icon (PNG variant).
 * @see https://lucide.dev/icons/git-pull-request-closed
 */
export default createPngIcon(require('../assets/git-pull-request-closed.png'), 'GitPullRequestClosedIcon');
