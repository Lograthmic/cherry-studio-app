import { createPngIcon } from '../createPngIcon';

/**
 * FolderKey icon (PNG variant).
 * @see https://lucide.dev/icons/folder-key
 */
export default createPngIcon(require('../assets/folder-key.png'), 'FolderKeyIcon');
