import { createPngIcon } from '../createPngIcon';

/**
 * Medal icon (PNG variant).
 * @see https://lucide.dev/icons/medal
 */
export default createPngIcon(require('../assets/medal.png'), 'MedalIcon');
