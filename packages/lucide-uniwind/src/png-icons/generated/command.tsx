import { createPngIcon } from '../createPngIcon';

/**
 * Command icon (PNG variant).
 * @see https://lucide.dev/icons/command
 */
export default createPngIcon(require('../assets/command.png'), 'CommandIcon');
