import { createPngIcon } from '../createPngIcon';

/**
 * CameraOff icon (PNG variant).
 * @see https://lucide.dev/icons/camera-off
 */
export default createPngIcon(require('../assets/camera-off.png'), 'CameraOffIcon');
