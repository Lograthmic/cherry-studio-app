import { createPngIcon } from '../createPngIcon';

/**
 * StickyNote icon (PNG variant).
 * @see https://lucide.dev/icons/sticky-note
 */
export default createPngIcon(require('../assets/sticky-note.png'), 'StickyNoteIcon');
