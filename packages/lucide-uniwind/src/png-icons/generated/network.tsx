import { createPngIcon } from '../createPngIcon';

/**
 * Network icon (PNG variant).
 * @see https://lucide.dev/icons/network
 */
export default createPngIcon(require('../assets/network.png'), 'NetworkIcon');
