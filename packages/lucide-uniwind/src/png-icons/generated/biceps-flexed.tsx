import { createPngIcon } from '../createPngIcon';

/**
 * BicepsFlexed icon (PNG variant).
 * @see https://lucide.dev/icons/biceps-flexed
 */
export default createPngIcon(require('../assets/biceps-flexed.png'), 'BicepsFlexedIcon');
