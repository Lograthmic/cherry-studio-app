import { createPngIcon } from '../createPngIcon';

/**
 * Wallet icon (PNG variant).
 * @see https://lucide.dev/icons/wallet
 */
export default createPngIcon(require('../assets/wallet.png'), 'WalletIcon');
