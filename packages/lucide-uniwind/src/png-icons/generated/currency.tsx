import { createPngIcon } from '../createPngIcon';

/**
 * Currency icon (PNG variant).
 * @see https://lucide.dev/icons/currency
 */
export default createPngIcon(require('../assets/currency.png'), 'CurrencyIcon');
