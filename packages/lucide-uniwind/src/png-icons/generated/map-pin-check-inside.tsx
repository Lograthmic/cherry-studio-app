import { createPngIcon } from '../createPngIcon';

/**
 * MapPinCheckInside icon (PNG variant).
 * @see https://lucide.dev/icons/map-pin-check-inside
 */
export default createPngIcon(require('../assets/map-pin-check-inside.png'), 'MapPinCheckInsideIcon');
