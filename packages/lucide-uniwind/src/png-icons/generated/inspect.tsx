import { createPngIcon } from '../createPngIcon';

/**
 * Inspect icon (PNG variant).
 * @see https://lucide.dev/icons/inspect
 */
export default createPngIcon(require('../assets/inspect.png'), 'InspectIcon');
