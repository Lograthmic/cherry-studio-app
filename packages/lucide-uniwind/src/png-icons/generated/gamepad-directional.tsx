import { createPngIcon } from '../createPngIcon';

/**
 * GamepadDirectional icon (PNG variant).
 * @see https://lucide.dev/icons/gamepad-directional
 */
export default createPngIcon(require('../assets/gamepad-directional.png'), 'GamepadDirectionalIcon');
