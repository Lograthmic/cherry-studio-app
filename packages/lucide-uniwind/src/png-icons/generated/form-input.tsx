import { createPngIcon } from '../createPngIcon';

/**
 * FormInput icon (PNG variant).
 * @see https://lucide.dev/icons/form-input
 */
export default createPngIcon(require('../assets/form-input.png'), 'FormInputIcon');
