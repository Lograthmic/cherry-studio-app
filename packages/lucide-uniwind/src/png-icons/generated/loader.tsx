import { createPngIcon } from '../createPngIcon';

/**
 * Loader icon (PNG variant).
 * @see https://lucide.dev/icons/loader
 */
export default createPngIcon(require('../assets/loader.png'), 'LoaderIcon');
