import { createPngIcon } from '../createPngIcon';

/**
 * Voicemail icon (PNG variant).
 * @see https://lucide.dev/icons/voicemail
 */
export default createPngIcon(require('../assets/voicemail.png'), 'VoicemailIcon');
