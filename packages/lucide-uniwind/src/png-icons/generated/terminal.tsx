import { createPngIcon } from '../createPngIcon';

/**
 * Terminal icon (PNG variant).
 * @see https://lucide.dev/icons/terminal
 */
export default createPngIcon(require('../assets/terminal.png'), 'TerminalIcon');
