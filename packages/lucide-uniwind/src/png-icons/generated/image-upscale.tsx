import { createPngIcon } from '../createPngIcon';

/**
 * ImageUpscale icon (PNG variant).
 * @see https://lucide.dev/icons/image-upscale
 */
export default createPngIcon(require('../assets/image-upscale.png'), 'ImageUpscaleIcon');
