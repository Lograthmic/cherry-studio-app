import { createPngIcon } from '../createPngIcon';

/**
 * Compass icon (PNG variant).
 * @see https://lucide.dev/icons/compass
 */
export default createPngIcon(require('../assets/compass.png'), 'CompassIcon');
