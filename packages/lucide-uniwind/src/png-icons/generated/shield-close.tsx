import { createPngIcon } from '../createPngIcon';

/**
 * ShieldClose icon (PNG variant).
 * @see https://lucide.dev/icons/shield-close
 */
export default createPngIcon(require('../assets/shield-close.png'), 'ShieldCloseIcon');
