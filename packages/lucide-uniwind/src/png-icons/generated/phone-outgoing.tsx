import { createPngIcon } from '../createPngIcon';

/**
 * PhoneOutgoing icon (PNG variant).
 * @see https://lucide.dev/icons/phone-outgoing
 */
export default createPngIcon(require('../assets/phone-outgoing.png'), 'PhoneOutgoingIcon');
