import { createPngIcon } from '../createPngIcon';

/**
 * Train icon (PNG variant).
 * @see https://lucide.dev/icons/train
 */
export default createPngIcon(require('../assets/train.png'), 'TrainIcon');
