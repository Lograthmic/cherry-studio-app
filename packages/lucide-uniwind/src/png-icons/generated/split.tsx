import { createPngIcon } from '../createPngIcon';

/**
 * Split icon (PNG variant).
 * @see https://lucide.dev/icons/split
 */
export default createPngIcon(require('../assets/split.png'), 'SplitIcon');
