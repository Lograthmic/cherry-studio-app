import { createPngIcon } from '../createPngIcon';

/**
 * PackageOpen icon (PNG variant).
 * @see https://lucide.dev/icons/package-open
 */
export default createPngIcon(require('../assets/package-open.png'), 'PackageOpenIcon');
