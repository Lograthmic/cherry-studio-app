import { createPngIcon } from '../createPngIcon';

/**
 * PhoneIncoming icon (PNG variant).
 * @see https://lucide.dev/icons/phone-incoming
 */
export default createPngIcon(require('../assets/phone-incoming.png'), 'PhoneIncomingIcon');
