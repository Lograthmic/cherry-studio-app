import { createPngIcon } from '../createPngIcon';

/**
 * Undo2 icon (PNG variant).
 * @see https://lucide.dev/icons/undo2
 */
export default createPngIcon(require('../assets/undo2.png'), 'Undo2Icon');
