import { createPngIcon } from '../createPngIcon';

/**
 * EarthLock icon (PNG variant).
 * @see https://lucide.dev/icons/earth-lock
 */
export default createPngIcon(require('../assets/earth-lock.png'), 'EarthLockIcon');
