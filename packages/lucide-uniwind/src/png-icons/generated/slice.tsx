import { createPngIcon } from '../createPngIcon';

/**
 * Slice icon (PNG variant).
 * @see https://lucide.dev/icons/slice
 */
export default createPngIcon(require('../assets/slice.png'), 'SliceIcon');
