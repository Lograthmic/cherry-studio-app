import { createPngIcon } from '../createPngIcon';

/**
 * PhoneForwarded icon (PNG variant).
 * @see https://lucide.dev/icons/phone-forwarded
 */
export default createPngIcon(require('../assets/phone-forwarded.png'), 'PhoneForwardedIcon');
