import { createPngIcon } from '../createPngIcon';

/**
 * FunnelX icon (PNG variant).
 * @see https://lucide.dev/icons/funnel-x
 */
export default createPngIcon(require('../assets/funnel-x.png'), 'FunnelXIcon');
