import { createPngIcon } from '../createPngIcon';

/**
 * ListRestart icon (PNG variant).
 * @see https://lucide.dev/icons/list-restart
 */
export default createPngIcon(require('../assets/list-restart.png'), 'ListRestartIcon');
