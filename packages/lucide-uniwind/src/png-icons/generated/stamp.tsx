import { createPngIcon } from '../createPngIcon';

/**
 * Stamp icon (PNG variant).
 * @see https://lucide.dev/icons/stamp
 */
export default createPngIcon(require('../assets/stamp.png'), 'StampIcon');
