import { createPngIcon } from '../createPngIcon';

/**
 * GlobeLock icon (PNG variant).
 * @see https://lucide.dev/icons/globe-lock
 */
export default createPngIcon(require('../assets/globe-lock.png'), 'GlobeLockIcon');
