import { createPngIcon } from '../createPngIcon';

/**
 * FolderClosed icon (PNG variant).
 * @see https://lucide.dev/icons/folder-closed
 */
export default createPngIcon(require('../assets/folder-closed.png'), 'FolderClosedIcon');
