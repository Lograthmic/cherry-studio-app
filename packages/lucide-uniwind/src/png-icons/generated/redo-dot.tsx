import { createPngIcon } from '../createPngIcon';

/**
 * RedoDot icon (PNG variant).
 * @see https://lucide.dev/icons/redo-dot
 */
export default createPngIcon(require('../assets/redo-dot.png'), 'RedoDotIcon');
