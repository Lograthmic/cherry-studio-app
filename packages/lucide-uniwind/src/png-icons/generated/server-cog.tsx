import { createPngIcon } from '../createPngIcon';

/**
 * ServerCog icon (PNG variant).
 * @see https://lucide.dev/icons/server-cog
 */
export default createPngIcon(require('../assets/server-cog.png'), 'ServerCogIcon');
