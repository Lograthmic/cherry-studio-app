import { createPngIcon } from '../createPngIcon';

/**
 * Calendars icon (PNG variant).
 * @see https://lucide.dev/icons/calendars
 */
export default createPngIcon(require('../assets/calendars.png'), 'CalendarsIcon');
