import { createPngIcon } from '../createPngIcon';

/**
 * Pipette icon (PNG variant).
 * @see https://lucide.dev/icons/pipette
 */
export default createPngIcon(require('../assets/pipette.png'), 'PipetteIcon');
