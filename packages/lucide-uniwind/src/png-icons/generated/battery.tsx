import { createPngIcon } from '../createPngIcon';

/**
 * Battery icon (PNG variant).
 * @see https://lucide.dev/icons/battery
 */
export default createPngIcon(require('../assets/battery.png'), 'BatteryIcon');
