import { createPngIcon } from '../createPngIcon';

/**
 * TimerOff icon (PNG variant).
 * @see https://lucide.dev/icons/timer-off
 */
export default createPngIcon(require('../assets/timer-off.png'), 'TimerOffIcon');
