import { createPngIcon } from '../createPngIcon';

/**
 * CalendarSync icon (PNG variant).
 * @see https://lucide.dev/icons/calendar-sync
 */
export default createPngIcon(require('../assets/calendar-sync.png'), 'CalendarSyncIcon');
