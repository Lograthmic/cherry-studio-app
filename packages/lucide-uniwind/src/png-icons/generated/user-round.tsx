import { createPngIcon } from '../createPngIcon';

/**
 * UserRound icon (PNG variant).
 * @see https://lucide.dev/icons/user-round
 */
export default createPngIcon(require('../assets/user-round.png'), 'UserRoundIcon');
