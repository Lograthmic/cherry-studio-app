import { createPngIcon } from '../createPngIcon';

/**
 * FolderSync icon (PNG variant).
 * @see https://lucide.dev/icons/folder-sync
 */
export default createPngIcon(require('../assets/folder-sync.png'), 'FolderSyncIcon');
