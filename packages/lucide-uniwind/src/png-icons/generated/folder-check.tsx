import { createPngIcon } from '../createPngIcon';

/**
 * FolderCheck icon (PNG variant).
 * @see https://lucide.dev/icons/folder-check
 */
export default createPngIcon(require('../assets/folder-check.png'), 'FolderCheckIcon');
