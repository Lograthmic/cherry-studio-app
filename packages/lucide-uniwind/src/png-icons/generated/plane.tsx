import { createPngIcon } from '../createPngIcon';

/**
 * Plane icon (PNG variant).
 * @see https://lucide.dev/icons/plane
 */
export default createPngIcon(require('../assets/plane.png'), 'PlaneIcon');
