import { createPngIcon } from '../createPngIcon';

/**
 * Handshake icon (PNG variant).
 * @see https://lucide.dev/icons/handshake
 */
export default createPngIcon(require('../assets/handshake.png'), 'HandshakeIcon');
