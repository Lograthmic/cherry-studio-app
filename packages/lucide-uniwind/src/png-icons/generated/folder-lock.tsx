import { createPngIcon } from '../createPngIcon';

/**
 * FolderLock icon (PNG variant).
 * @see https://lucide.dev/icons/folder-lock
 */
export default createPngIcon(require('../assets/folder-lock.png'), 'FolderLockIcon');
