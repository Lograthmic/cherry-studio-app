import { createPngIcon } from '../createPngIcon';

/**
 * ClipboardCheck icon (PNG variant).
 * @see https://lucide.dev/icons/clipboard-check
 */
export default createPngIcon(require('../assets/clipboard-check.png'), 'ClipboardCheckIcon');
