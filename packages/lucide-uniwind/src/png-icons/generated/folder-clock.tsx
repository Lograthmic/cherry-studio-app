import { createPngIcon } from '../createPngIcon';

/**
 * FolderClock icon (PNG variant).
 * @see https://lucide.dev/icons/folder-clock
 */
export default createPngIcon(require('../assets/folder-clock.png'), 'FolderClockIcon');
