import { createPngIcon } from '../createPngIcon';

/**
 * StepBack icon (PNG variant).
 * @see https://lucide.dev/icons/step-back
 */
export default createPngIcon(require('../assets/step-back.png'), 'StepBackIcon');
