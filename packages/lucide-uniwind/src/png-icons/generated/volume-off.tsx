import { createPngIcon } from '../createPngIcon';

/**
 * VolumeOff icon (PNG variant).
 * @see https://lucide.dev/icons/volume-off
 */
export default createPngIcon(require('../assets/volume-off.png'), 'VolumeOffIcon');
