import { createPngIcon } from '../createPngIcon';

/**
 * FolderRoot icon (PNG variant).
 * @see https://lucide.dev/icons/folder-root
 */
export default createPngIcon(require('../assets/folder-root.png'), 'FolderRootIcon');
