import { createPngIcon } from '../createPngIcon';

/**
 * ReplyAll icon (PNG variant).
 * @see https://lucide.dev/icons/reply-all
 */
export default createPngIcon(require('../assets/reply-all.png'), 'ReplyAllIcon');
