import { createPngIcon } from '../createPngIcon';

/**
 * Folder icon (PNG variant).
 * @see https://lucide.dev/icons/folder
 */
export default createPngIcon(require('../assets/folder.png'), 'FolderIcon');
