import { createPngIcon } from '../createPngIcon';

/**
 * Piano icon (PNG variant).
 * @see https://lucide.dev/icons/piano
 */
export default createPngIcon(require('../assets/piano.png'), 'PianoIcon');
