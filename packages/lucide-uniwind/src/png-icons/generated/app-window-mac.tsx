import { createPngIcon } from '../createPngIcon';

/**
 * AppWindowMac icon (PNG variant).
 * @see https://lucide.dev/icons/app-window-mac
 */
export default createPngIcon(require('../assets/app-window-mac.png'), 'AppWindowMacIcon');
