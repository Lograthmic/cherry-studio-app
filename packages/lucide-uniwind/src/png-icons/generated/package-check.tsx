import { createPngIcon } from '../createPngIcon';

/**
 * PackageCheck icon (PNG variant).
 * @see https://lucide.dev/icons/package-check
 */
export default createPngIcon(require('../assets/package-check.png'), 'PackageCheckIcon');
