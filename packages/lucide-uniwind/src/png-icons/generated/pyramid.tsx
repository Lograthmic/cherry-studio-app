import { createPngIcon } from '../createPngIcon';

/**
 * Pyramid icon (PNG variant).
 * @see https://lucide.dev/icons/pyramid
 */
export default createPngIcon(require('../assets/pyramid.png'), 'PyramidIcon');
