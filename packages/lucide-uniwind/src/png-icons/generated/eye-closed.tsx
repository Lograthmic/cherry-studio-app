import { createPngIcon } from '../createPngIcon';

/**
 * EyeClosed icon (PNG variant).
 * @see https://lucide.dev/icons/eye-closed
 */
export default createPngIcon(require('../assets/eye-closed.png'), 'EyeClosedIcon');
