import { createPngIcon } from '../createPngIcon';

/**
 * Container icon (PNG variant).
 * @see https://lucide.dev/icons/container
 */
export default createPngIcon(require('../assets/container.png'), 'ContainerIcon');
