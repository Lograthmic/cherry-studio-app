import { createPngIcon } from '../createPngIcon';

/**
 * TestTubes icon (PNG variant).
 * @see https://lucide.dev/icons/test-tubes
 */
export default createPngIcon(require('../assets/test-tubes.png'), 'TestTubesIcon');
