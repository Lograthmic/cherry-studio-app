import { createPngIcon } from '../createPngIcon';

/**
 * AudioLines icon (PNG variant).
 * @see https://lucide.dev/icons/audio-lines
 */
export default createPngIcon(require('../assets/audio-lines.png'), 'AudioLinesIcon');
