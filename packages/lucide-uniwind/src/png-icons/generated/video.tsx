import { createPngIcon } from '../createPngIcon';

/**
 * Video icon (PNG variant).
 * @see https://lucide.dev/icons/video
 */
export default createPngIcon(require('../assets/video.png'), 'VideoIcon');
