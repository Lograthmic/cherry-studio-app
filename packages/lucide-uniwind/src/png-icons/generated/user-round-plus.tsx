import { createPngIcon } from '../createPngIcon';

/**
 * UserRoundPlus icon (PNG variant).
 * @see https://lucide.dev/icons/user-round-plus
 */
export default createPngIcon(require('../assets/user-round-plus.png'), 'UserRoundPlusIcon');
