import { createPngIcon } from '../createPngIcon';

/**
 * DatabaseZap icon (PNG variant).
 * @see https://lucide.dev/icons/database-zap
 */
export default createPngIcon(require('../assets/database-zap.png'), 'DatabaseZapIcon');
