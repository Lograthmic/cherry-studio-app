import { createPngIcon } from '../createPngIcon';

/**
 * SwitchCamera icon (PNG variant).
 * @see https://lucide.dev/icons/switch-camera
 */
export default createPngIcon(require('../assets/switch-camera.png'), 'SwitchCameraIcon');
