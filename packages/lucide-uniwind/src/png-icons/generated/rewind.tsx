import { createPngIcon } from '../createPngIcon';

/**
 * Rewind icon (PNG variant).
 * @see https://lucide.dev/icons/rewind
 */
export default createPngIcon(require('../assets/rewind.png'), 'RewindIcon');
