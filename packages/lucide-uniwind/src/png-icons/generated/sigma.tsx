import { createPngIcon } from '../createPngIcon';

/**
 * Sigma icon (PNG variant).
 * @see https://lucide.dev/icons/sigma
 */
export default createPngIcon(require('../assets/sigma.png'), 'SigmaIcon');
