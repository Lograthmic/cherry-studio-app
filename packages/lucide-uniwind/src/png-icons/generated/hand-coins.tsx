import { createPngIcon } from '../createPngIcon';

/**
 * HandCoins icon (PNG variant).
 * @see https://lucide.dev/icons/hand-coins
 */
export default createPngIcon(require('../assets/hand-coins.png'), 'HandCoinsIcon');
