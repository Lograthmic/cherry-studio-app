import { createPngIcon } from '../createPngIcon';

/**
 * Proportions icon (PNG variant).
 * @see https://lucide.dev/icons/proportions
 */
export default createPngIcon(require('../assets/proportions.png'), 'ProportionsIcon');
