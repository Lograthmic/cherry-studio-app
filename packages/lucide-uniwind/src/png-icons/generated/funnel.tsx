import { createPngIcon } from '../createPngIcon';

/**
 * Funnel icon (PNG variant).
 * @see https://lucide.dev/icons/funnel
 */
export default createPngIcon(require('../assets/funnel.png'), 'FunnelIcon');
