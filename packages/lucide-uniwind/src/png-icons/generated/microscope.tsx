import { createPngIcon } from '../createPngIcon';

/**
 * Microscope icon (PNG variant).
 * @see https://lucide.dev/icons/microscope
 */
export default createPngIcon(require('../assets/microscope.png'), 'MicroscopeIcon');
