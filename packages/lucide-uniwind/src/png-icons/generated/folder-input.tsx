import { createPngIcon } from '../createPngIcon';

/**
 * FolderInput icon (PNG variant).
 * @see https://lucide.dev/icons/folder-input
 */
export default createPngIcon(require('../assets/folder-input.png'), 'FolderInputIcon');
