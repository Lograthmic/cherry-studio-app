import { createPngIcon } from '../createPngIcon';

/**
 * Filter icon (PNG variant).
 * @see https://lucide.dev/icons/filter
 */
export default createPngIcon(require('../assets/filter.png'), 'FilterIcon');
