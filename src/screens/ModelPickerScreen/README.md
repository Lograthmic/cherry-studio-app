# Model Picker Screen

This module owns the model picker route implementation.

## Public Interface

- `ModelPickerScreen` is exported from `index.ts`.

## Organization

- Reusable model picker UI and model selection helpers live in `src/components/modelPicker`.
- The route adapter in `src/app/model-picker.tsx` imports this screen from the module root.
