# Navigation

This module owns React Navigation theme integration for the app shell.

## Public Interface

- `NavigationThemeProvider` is exported from `index.ts`.

## Organization

- `components/` contains the provider implementation.
